---
id: checkpoint_30ffd5ea
timestamp: 2026-09-21T23:38:00.976Z
tags:
  - docs
  - qt-qml
  - readme
  - v1.5.0
git:
  branch: main
  commit: 18fcc90
  worktree: /home/murphy/source/code-kb
  files:
    - docs/plans/025-qt-qml-first-class-support.md
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: README gains a "Qt and QML" section
type: checkpoint
next: Site page, agent guidelines, and the v1.5.0 release notes (task 3)
---

## README gains a "Qt and QML" section

Task B8 step 2. Added `## Qt and QML` to `README.md` before `## MCP Tool Catalog`.

- One paragraph on what code-kb does for a Qt developer: object-tree skeletons, `extends` rows, qualified names, singleton readers, `handler` and `handler (candidate)` labels, `qmldir`, `.qmltypes`, Qt JavaScript with `.pragma library`, and the hidden `autotests/` and `tst_*.qml` files. Names julie-extract 3.2.0 as the requirement.
- The Part C honest claim sentence, verbatim.
- A "Validated on" table with the four pinned corpora and their QML file counts.
- Six commands with the results measured in this task.
- Four known limits: Qt C++ macro gaps, no `.ui` or CMake files, Qt-module aliases never resolve, and the 200-row reference cap.

**All numbers came from runs against Omarchy `49306774` and Kirigami `ca7d636`** with `target/release/code-kb` at `28ce7c3` and julie-extract 3.2.0.
