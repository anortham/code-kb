---
id: checkpoint_d683afb9
timestamp: 2026-09-21T04:07:00.956Z
tags:
  - plan-023
  - search
  - eval
git:
  branch: main
  commit: f111672
  worktree: /home/murphy/source/code-kb
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Plan 023 groundwork: held-out set 3 sealed, offline lever simulation"
type: milestone
---

## Plan 023 groundwork: held-out set 3 sealed, offline lever simulation

**WHAT**: `~/.code-kb/search-eval/acceptance-3.json` (40 README-worded cases, 10 per repo) written by an opus agent and sealed (sha 87e13fee...dd27) before any plan 023 code change. `acceptance-2.json` retired into `development-3.json` (same 40 cases, ids `*-d3-*`) so its held-out failures can be studied; README in the eval dir records this. Offline simulator in the scratchpad (`collect.py` caches limit-200 explain rows + body tokens; `sim.py` rescoring; `bodyfts.py` builds scratch FTS5 body-word indexes per repo (definition kinds only: hermes 58k rows/36 MB, miller 11k/9 MB) and simulates a body recall branch).

**WHY**: Promoted v1.3 build is below v1.2.0 on sealed acceptance-2 (18/27/12/22 vs 19/27/15/22); tuning-set gains did not carry. Misses classified: function words ("was", "how", "so", "whether") earn credit because IDF over code text makes them rare; README vocabulary lives in bodies, not name/sig/doc.

**HOW (findings so far on dev+dev-2 rank-only sim)**: parent/file context lever: zero effect. Extended stop list: +2 sym@1 dev-2, -2 sym@3. Body words credit 0.5 for uncovered terms: +3 sym@1 / +4 file@1 dev-2, +1 dev, no losses; credit 1 hurts. Recall branch simulation running.

**IMPACT**: Next: read body-branch sim results, then write plan 023 (stop words + body-word FTS index built by code-kb after scan/reconcile + body recall branch + credit 0.5), implement, measure on dev/dev-2/dev-3 vs v1.2.0, run acceptance-3 once at the end. No release until better than v1.2.0 on held-out wording.
