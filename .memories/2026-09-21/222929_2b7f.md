---
id: checkpoint_2b7f6ad9
timestamp: 2026-09-21T22:29:29.786Z
tags:
  - qml
  - qt
  - plan-025
  - execution
  - wave1
git:
  branch: main
  commit: 18fcc90
  files:
    - docs/plans/025-qt-qml-first-class-support.md
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Wave 1 merged in both repos (plan 025 execution)
type: checkpoint
next: Review lane E/F reports when wave 2 completes; review the one-line
  skeleton fix; then launch julie wave 3 (integration) and code-kb wave 2
  (extends walk, handler labels, docs).
---

## Wave 1 merged in both repos (plan 025 execution)

**julie-extractors:** four lanes reviewed and merged into `feat/qt-first-class` (worktree `.worktrees/feat-qt-first-class`, HEAD 310ca967): lane A identifier metadata (`BaseExtractor::create_identifier_with_metadata`, keys reach `identifiers.metadata_json`, extractor keys win), lane B qmldir type rows kind `export`, lane C Qt JS `.pragma`/`.import` pre-pass shared by pipeline and check paths, lane D inline components, signal signatures + parameters, `pragma Singleton` + `qml.pragma.v1` fact, bare `required`, multi-line property heads. Default + qml + qmldir + javascript tiers green on the merge. Lane worktrees removed, branches qt/lane-a..d kept. Open for the integration lane: capabilities.json (qmldir class→export, qml.pragma.v1, register fixtures qml/qt_symbols and javascript/qml_directives), registry spec for qml.pragma.v1 and javascript.qml_directive.v1 + contract JSON regen, output-change ledger + epoch, docs; lane C's registry adapter assigns `results.structural_facts =` (should extend). Wave 2 (lane E object model → lane F references) running as workflow wf_a29ff32a-bc8.

**code-kb:** two lanes reviewed and merged into `feat/v1.5-qt` (worktree `.claude/worktrees/wf_be0f6c05-f1d-2`, HEAD 9047940): B4 tst_/autotests test paths (guard + predicate + Rust rule), B5 aliases signal/import/binding/component/module/pragma, B3 type targets match `metadata_json.receiver` rows (refs Color/Style now hit the cap), B2 import alias satisfies a pending receiver (Kirigami refs Page 20→66 rows; known limit: QQC2.Page resolves to local Page.qml), B1 skeleton containers for any non-body-hiding symbol with children. core + cli + clippy green. Fix in flight: one-line symbols with children must stay leaves (Cargo.toml skeleton grew 40→88). Deferred: CLAUDE.md/AGENTS.md invariant 6 alias text (docs lane B8); the identifier owner-check alias branch was measured as no-op and skipped.

**Still to do:** julie wave 3 integration; code-kb B2 extends-walk after julie 3.2.0 builds, B6 handler candidate labels, B7 pin bump (needs the julie release), B8 docs; branch gates; adversarial review; release approvals (owner).
