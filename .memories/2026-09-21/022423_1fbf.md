---
id: checkpoint_1fbfbd28
timestamp: 2026-09-21T02:24:23.228Z
git:
  branch: main
  commit: f111672
  worktree: /home/murphy/source/code-kb
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: WHAT
---

## WHAT

- Bumped 1.2.0 to 1.3.0 in `Cargo.toml`, `crates/code-kb-cli/Cargo.toml`, `Cargo.lock`, `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `.codex-plugin/plugin.json`, `plugin.json`, `.github/workflows/release-binaries.yml`, and `docs/site/index.html`.
- Wrote `docs/release-notes/v1.3.0.md` in the v1.2.0 format: Highlights, Search, Evaluation, Compatibility.
- Extended the Core Invariant 6 "Search ranking" bullet in `CLAUDE.md` and `AGENTS.md` with the test-file path rule and the name-strength tie-break.
- Corrected the `search_symbols` and `lookup_symbol` rows in `README.md`: test files are hidden unless `is_test` is true; `search_symbols` now lists `is_test`.

## WHY

Plan 021 shipped two search changes on `feat/search-v1.3`. Task 6 turns the branch into a release-ready tree. The user decides afterwards whether to tag and publish.

## HOW

- Read the plan Results section for every number. No evaluation was re-run.
- Read the shipped rule from `is_test_path` and `test_path_predicate` in `crates/code-kb-core/src/queries.rs`, and `name_strength` from `SearchExplain` in `models.rs`.
- The worktree code-kb index is stale for `queries.rs`; the indexed `is_test_path` body still shows the pre-task-2 rule. The rule text came from the working tree.

## IMPACT

- `cargo build --release` gives `code-kb 1.3.0`.
- `node --test tests/plugin/*.test.cjs`: 16 pass.
- `cargo test -p code-kb-cli --test cli_test --test mcp_test`: 34 + 11 pass.
- `cargo fmt --all -- --check` and `cmp AGENTS.md CLAUDE.md`: clean.
- `scripts/release-preflight.sh`: steps 1 to 7 pass, stops at step 8 (needs a pushed HEAD and green CI). That step is the lead's.

