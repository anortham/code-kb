---
id: checkpoint_5c09ccd0
timestamp: 2026-09-21T01:38:57.754Z
tags:
  - plan-021
  - search
  - evaluation
git:
  branch: main
  commit: f111672
  worktree: /home/murphy/source/code-kb
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: WHAT
---

## WHAT

- Wrote `development-2.json` (60 cases, 15 per repository) and `acceptance-2.json` (40 cases, 10 per repository) in `~/.code-kb/search-eval`, outside every checkout.
- Sealed `acceptance-2.json` read-only with `acceptance-2.sha256` and recorded the hash in the evaluation README.
- Added two per-case columns to `runner.py`: `admitted` (rank of the expected symbol among all 200 candidates) and `branches` (the recall branches of that row). The summary gained a `notAdm` column.
- Pointed `DEFAULT_BIN` at the feat-search-v1.3 worktree build and copied the released v1.2.0 binary to `bin/code-kb-1.2.0`.
- Recorded the v1.2.0 baseline on the new development set in the plan and the README.

## WHY

Every later plan 021 task is measured against these sets. The plan 019 rule keeps the query text out of every checkout, so the sets and the runner live in the home directory. The admission column separates a recall miss from a ranking miss, which decides whether a new recall branch or a rerank change is the right fix.

## HOW

- Read each repository README and docs, then chose answer symbols from code-kb outlines and skeletons, not from search output alone.
- Proved every expected symbol with `runner.py --check-labels`: 0 missing across both sets.
- One extra search per case supplies the new columns; the limit-10 search still supplies the ranks and the timing.

## IMPACT

- v1.2.0 baseline on the new development set: 29 of 60 file@1, 25 of 60 symbol@1, 35 of 60 symbol@3, 5 of 60 not admitted.
- The plan target of at most 2 not admitted needs recall work, not only ranking work.
- The plan 020 result files were not overwritten: seven ranks moved because the four repositories changed on disk between the two runs. The reruns are kept beside them under a `-rerun` name.
- The regression gate on the v1.2.0 binary still reports no case worse than the baseline.
