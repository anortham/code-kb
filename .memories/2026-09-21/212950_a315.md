---
id: checkpoint_a3156679
timestamp: 2026-09-21T21:29:50.617Z
tags:
  - qml
  - qt
  - audit
  - plan-025
  - julie-extractors
git:
  branch: main
  commit: 18fcc90
  files:
    - docs/plans/025-qt-qml-first-class-support.md
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Qt/QML first-class support audit → plan 025 (draft)
type: checkpoint
next: Owner reviews plan 025 and decides on A2 (drop plain binding symbols) and
  release sequencing; then execute Part A in julie-extractors first.
unknowns:
  - "Whether the owner keeps binding symbols (A2 alternative: code-kb hides them
    in skeletons)"
  - Exact code path in find_references_internal for type_usage candidates (B2) —
    verify before changing
---

## Qt/QML first-class support audit → plan 025 (draft)

**WHAT:** Audited code-kb v1.4.1 + julie-extract 3.1.3 QML support end to end on four corpora (Omarchy shell 106 QML, Kirigami 225, plasma-workspace 222, Quickshell examples 14). Wrote `docs/plans/025-qt-qml-first-class-support.md` (evidence, gaps G1-G14, Parts A/B/C, verification commands) and the companion extractor task list `~/source/julie-extractors/docs/plans/2026-09-21-qt-first-class-gap-closure.md`. No code changed.

**WHY:** the owner wants to promote code-kb to KDE / Omarchy / Quickshell developers with a defensible "first-class support" claim.

**HOW:** live CLI checks (`scan`, `skeleton`, `refs`, `lookup`, `search`, `blast-radius`, `context`, `edit`, `edit-file`, `facts`), SQLite queries on the four indexes, and `julie-extract check` on every Omarchy QML and JS file; grammar node list from the pinned tree-sitter-qmljs checkout.

- Works today: all 567 QML files parse; component/property/function/signal/import/id/handler symbols; qmldir modules; facts; validated edits; blast radius through instantiations; Qt Quick Test detection in the extractor.
- Blocking gaps: skeleton flattens nested objects (Button.qml 209→132 lines only); inline components never extracted (48/21/17 in source, 0 symbols); qualified type names (`Kirigami.Page`, 33% of Kirigami usages) never match; root base type and singleton receivers emit no reference (`refs Color`, `refs BarWidget` = 0); signals lack signatures and handler links; qmldir rows duplicate classes; `tst_`/`autotests/` not in the code-kb test path rule; `.pragma library` JS fails parse and blocks edits; Qt C++ macros wreck headers (Kirigami 981 diagnostics in 70/102 files, 0 property symbols for 297 Q_PROPERTY; plasma-workspace 4,502 in 636/1,030).

**IMPACT:** sequencing proposed as julie 3.2.0 (QML/qmldir/Qt JS) → code-kb v1.5.0 (skeleton nesting, base-type resolution, test paths, aliases, docs) → julie 3.3.0 (Qt C++ macros). Honest claim until then: "first-class QML and Quickshell support; Qt C++ headers indexed with known macro gaps". Side effect: `~/source/omarchy/.code-kb` index now exists (gitignored by code-kb).
