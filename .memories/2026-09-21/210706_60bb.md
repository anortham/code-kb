---
id: checkpoint_60bb0bab
timestamp: 2026-09-21T21:07:06.931Z
tags:
  - release
  - v1.4.1
  - performance
git:
  branch: main
  commit: a5f4c22
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "v1.4.1 released: lookup and telemetry latency fixes"
next: Retire the stale eval labels (kb-d2-word-rarity, kb-acc2-rerank); consider
  adding a previous-release benchmark comparison to release-preflight.sh.
---

## v1.4.1 released: lookup and telemetry latency fixes

**WHAT:** `fix/lookup-latency` reviewed by Codex (one medium finding: `count_file_symbols` ignored letter case where `load_file_symbols` prefers the exact-case path; fixed with an exact-first count and a test), merged fast-forward into main, released as v1.4.1 (tag at 0cfcf5f: fix commits 20d8464 + 02b425f, release bump f3cc1a7, cargo fmt 0cfcf5f). GitHub Release with six archives, crates.io `code-kb-core` and `code-kb-cli` 1.4.1, checksums in `docs/release-notes/v1.4.1.md`.

**Gates:** local preflight green after a cargo fmt commit; Windows guest ran `queries` and `mcp_test` (the guest needed `scripts/restore-julie-extract.ps1`, it held julie-extract 3.1.1); GitHub CI green on the tagged commit before the tag.

**Numbers (MCP p50):** lookup_symbol 10.6 -> 2.2 ms, file_skeleton 10.6 -> 6.1 ms, fresh CLI median 21.8 -> 14.9 ms. Search ranks unchanged.

**IMPACT:** release binaries for A/B kept at `~/.code-kb/search-eval/bin/code-kb-1.3.0`, `-1.3.1`, `-1.4.0`, `-1.4.1`. Next release: run `scripts/benchmark_quality.py` against the previous release binary as part of preflight.
