---
id: checkpoint_3ca13c21
timestamp: 2026-09-21T22:06:14.206Z
tags:
  - qml
  - qt
  - plan-025
  - execution
  - workflow
git:
  branch: main
  commit: 18fcc90
  files:
    - docs/plans/025-qt-qml-first-class-support.md
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Plan 025 execution started (ultracode workflows, Opus implementers,
  lead review)
type: checkpoint
next: Review wave-1 lane reports, merge lanes into feat/qt-first-class, launch
  wave 2 (object model → references).
---

## Plan 025 execution started (ultracode workflows, Opus implementers, lead review)

**WHAT:** Owner approved plan 025 rev 2 ("make it happen"); A2 (drop plain binding symbols) taken as accepted since it was the recommendation. Execution uses Workflow runs of Opus agents with the lead reviewing between waves.

**Worktrees (julie-extractors):** feature worktree `.worktrees/feat-qt-first-class` (branch `feat/qt-first-class`, base caf1d1d0, QML + qmldir baseline green). Wave-1 lanes off that branch: `.worktrees/qt-lane-a-identifier-metadata` (qt/lane-a, task 8), `qt-lane-b-qmldir-export` (qt/lane-b, task 13), `qt-lane-c-qtjs-directives` (qt/lane-c, task 14), `qt-lane-d-qml-symbols` (qt/lane-d, tasks 3-7). Workflow run wf_56c5f1d0-c68 (script julie-qt-wave1). Lanes commit on their own branches (serial-worker-commit); the lead merges into feat/qt-first-class after inline review.

**Next julie waves:** wave 2 serial in the feature worktree: tasks 1-2 (object model) then tasks 9-12 (references, needs lane A's metadata field). Wave 3: integration (tasks 15-18: goldens, capabilities, contract ledger, epoch, docs). Then lead branch gate + adversarial review workflow.

**code-kb:** B2 (alias part), B3, B4, B5 on queries.rs and B1 on formatters.rs do not depend on the extractor and start now in Workflow worktrees. B2 extends edges, B6, B7 pin bump, B8 docs come after julie 3.2.0 exists.

**Release boundary:** no push, tag, or publish without the owner; releases are julie 3.2.0 → code-kb 1.5.0 → julie 3.3.0 → code-kb 1.6.0.
