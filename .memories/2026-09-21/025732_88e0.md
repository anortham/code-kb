---
id: checkpoint_88e0657f
timestamp: 2026-09-21T02:57:32.130Z
tags:
  - plan-022
  - search
  - v1.3
  - codex-review
git:
  branch: main
  commit: f111672
  worktree: /home/murphy/source/code-kb
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Plan 022 opened after the Codex second opinion on plan 021
type: decision
decision: Continue search work on feat/search-v1.3 with plan 022 (distinct-term
  scoring); v1.3.0 ships only when it beats v1.2.0 on every set.
alternatives:
  - "Ship v1.3.0 as the test-file fix only: rejected by the owner, not better
    than 1.2"
  - "Stop investing in README-phrased search: rejected by the owner"
  - "Path/parent context branch first: lead's offline count shows it adds unseen
    words in 5 of 61 misses, so it is the second lever, not the first"
next: Review task 7 and task 8 reports; apply labels; rerun baselines; decide
  the default scorer.
---

## Plan 022 opened after the Codex second opinion on plan 021

**WHAT:** docs/plans/022-search-v1.3-distinct-term-scoring.md. Codex (read-only, user-requested) diagnosed the 61 development misses: the answer is usually a candidate that already holds the query words and loses on score; the rerank sums per-field coverage so repeated words beat complementary words; candidate-sample rarity inflates prose words; the `admitted` diagnostic measures a different search because caps scale with `--limit`; ~15 labels have a plausible competing answer; the test-path rule leaked directory names into file-name rules (fixed in 20d7ccd).

**WHY:** The owner's gate: v1.3 does not ship until it beats v1.2.0; keep making search better.

**HOW:** Task 7 (label review, eval dir only) and task 8 (distinct-term scorer behind CODE_KB_RERANK, variants uniform and rarity, measured on identical candidates) run in parallel on opus. Lead then applies labels, reruns v1.2.0 and each scorer, promotes the winner, removes the switch, updates corpus tests and docs.

**IMPACT:** The Codex opinion and the miss dump live at ~/.code-kb/search-eval/reviews/ (they name expected answers, so never inside a checkout).
