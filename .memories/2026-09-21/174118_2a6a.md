---
id: checkpoint_2a6a9864
timestamp: 2026-09-21T17:41:18.633Z
tags:
  - release
  - v1.4.0
  - edit_file
  - codex-review
  - plan-024
git:
  branch: main
  commit: 0ee82c6
  worktree: /home/murphy/source/code-kb
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Plan 024 Part B (v1.4.0) complete on feat/v1.4, awaiting release approval
type: checkpoint
next: "Owner approves v1.4.0 release: fast-forward main to feat/v1.4, push,
  green CI, release-preflight.sh, tag, cargo publish core then cli, record
  checksums."
---

## Plan 024 Part B (v1.4.0) complete on feat/v1.4, awaiting release approval

**WHAT:** Eight workflow tasks plus three lead commits. `edit_file` tool and CLI, not-found recovery with candidates, `CATEGORY_ALIASES` for facts, tokens-saved baseline for every read tool with coverage, docs, v1.4.0 bump. Branch rebased onto `main` 0ee82c6 (v1.3.1 checksums). HEAD b778619.

**WHY:** Owner-approved plan (docs/plans/024-...). Codex pre-merge review (general + security) returned three distinct findings, all real, all fixed red-then-green in b778619:
- `exact_matches` cloned the replacement per match before `occurrence` applied (4 GiB on a 1 MiB file of one char). Now offsets only, result capped at 8 MiB (`EditTooLarge`).
- Overlapping matches were skipped (`ana` in `banana`), so `only` passed and `last` edited the wrong place. Overlapping starts found; `all` replaces non-overlapping ones; ambiguity message lists ten lines then a count.
- Failed-edit error quoted three file lines and the server wrote it verbatim to telemetry. Telemetry now records the first line of an error only.
- Lead fix dbbcd43: touched list prefers `DEFINITION_KINDS` over a local variable.

**HOW:** Workflow `plan-024-part-b` with one Opus agent per task in series; Codex via scratchpad `premerge.sh` with `GCM_CREDENTIAL_STORE` unset (razorback redactor false positive).

**IMPACT:** Gate on b778619: 375 workspace tests, clippy/fmt clean, 17 plugin tests. Campaign clean, 2/2 invocations. Report at `.memories/autonomous-run-2026-09-21-plan-024-part-b.md`. Next: owner approval to fast-forward main, push, CI, preflight, tag v1.4.0, publish crates, record checksums. Owner should confirm the regression-set reading (25 vs 26 file@1, case `concept-fts`, same on v1.3.0 binary today).
