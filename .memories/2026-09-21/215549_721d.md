---
id: checkpoint_721d85bb
timestamp: 2026-09-21T21:55:49.528Z
tags:
  - qml
  - qt
  - plan-025
  - codex-review
git:
  branch: main
  commit: 18fcc90
  files:
    - docs/plans/025-qt-qml-first-class-support.md
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Codex review of plan 025 folded in (revision 2)
type: decision
decision: Plan 025 and the julie companion are revised to revision 2 with every
  verified Codex correction; the singleton gap moves to code-kb (match
  member-access receiver metadata) and the qualified-name gap becomes a code-kb
  resolver change plus a small extractor change.
alternatives:
  - "Keep the extractor-side capitalized-receiver identifier rule (rejected:
    code-kb already stores the source receiver on member-access rows)"
  - "Keep B5 export-row suppression (rejected: single-symbol resolution already
    prefers definition kinds; lookup listing both rows is honest)"
next: Owner approves plan 025 revision 2 and decides task 2 / A2 (drop plain
  binding symbols); then execute julie 3.2.0 tasks 1-18 first.
---

## Codex review of plan 025 folded in (revision 2)

**WHAT:** Codex (`codex exec`, read-only) reviewed both draft plans. Every factual disagreement was verified against the indexes and source and folded in: `BorderGeometry.js` is indexed (264 symbols) and only edits fail; 12 `BarWidget` and 10 `Panel` extending roots (not five); 38 `Q_PROPERTY` in `columnview.h` (not 17); three `waitForWindowActive` rows; two template diagnostics qualify the parse claim; corpus commits pinned (Kirigami ca7d636, plasma-workspace a45871a, quickshell-examples c6d1236).

**WHY:** the plans had design holes that would have failed their own acceptance criteria.

**HOW (accepted design changes):**
- Nested objects become `field` symbols (code-kb skips `variable`, cuts signatures at `{`, and excludes `variable` from identifier containment); signature `id: Type`; root id row kept; relationship `from` never the object row.
- One reference contract: terminal-name identifiers with receiver metadata (mapper change), a pending `extends` row for the root base type (blast radius reads relationships/pending only), handler identifiers with owner receivers and candidate labelling, attached binding names, `onXChanged`.
- Singleton references: code-kb caller query matches `metadata_json.receiver` for type targets (249 rows/53 files for `Color` already present). Extractor rule dropped.
- Qualified names: code-kb `pending_target_predicate` accepts import aliases as receivers; extractor only aligns identifier names with the pending rows.
- Preprocessing for `.pragma`/`.import` and Qt C++ macros must be shared with the `check` path; token-level, byte-preserving; `public Q_SLOTS:` handled.
- Test path rule: guard + predicate + `tst_`/`autotests/`; exact lookup keeps test rows by contract.
- Release chain: julie 3.2.0 → code-kb 1.5.0 → julie 3.3.0 → code-kb 1.6.0, epoch advance on both julie releases, six checksums per pin bump.
- New gaps G15 (bare `required`), G16 (attached bindings, `Behavior on`), G17 (`.ui`/CMake out of claim).

**IMPACT:** plans are ready for the owner's approval; review archived at `~/.code-kb/search-eval/reviews/2026-09-21-codex-plan-025-qt-qml-review.md`. Still no code changes.
