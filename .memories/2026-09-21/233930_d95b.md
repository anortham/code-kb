---
id: checkpoint_d95befca
timestamp: 2026-09-21T23:39:30.711Z
tags:
  - docs
  - qt-qml
  - release-notes
  - v1.5.0
  - agents-md
git:
  branch: main
  commit: 18fcc90
  worktree: /home/murphy/source/code-kb
  files:
    - docs/plans/025-qt-qml-first-class-support.md
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Site entry, agent guideline rules, and v1.5.0 release notes
type: checkpoint
next: Report to the lead; the pin bump (B7) still waits for the julie 3.2.0
  release assets
---

## Site entry, agent guideline rules, and v1.5.0 release notes

Task B8 step 3, the last one.

- **`docs/site/index.html`**: added a `Qt and QML` entry to the feature list naming QML, `qmldir`, `.qmltypes`, Qt JavaScript, Qt C++, and the `.ui`/CMake exclusion. The list heading changed from "Seven capabilities" to "Eight capabilities", since the entry is the eighth.
- **`CLAUDE.md` and `AGENTS.md`, invariant 6**: the `Structural-fact categories:` alias list now names every alias in `CATEGORY_ALIASES`, read from `crates/code-kb-core/src/queries.rs`. A new `Reference rules:` bullet states the six rules this branch added.
- **`docs/release-notes/v1.5.0.md`**: follows the v1.4.1 structure. Requires julie-extract 3.2.0 with the pin bump pending; the eight user-facing changes; the measured table; the known limits; the compatibility notes. No checksums or dates invented.

**Verified:**
- `cargo test -p code-kb-cli --test mcp_test`: 15 passed.
- `cargo test -p code-kb-cli --test cli_test test_guidance_docs_use_canonical_parameter_names`: passed.
- `cargo test -p code-kb-cli --test cli_test agents` (`test_agents_and_claude_md_sync_contract`): passed.
- `cmp CLAUDE.md AGENTS.md`: identical. No `scripts/check-agent-doc-sync.sh` exists.
- `tools/list` over stdio returns 12 tools, which is the count the release notes claim.
