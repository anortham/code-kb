---
id: checkpoint_09c73011
timestamp: 2026-09-21T23:20:09.740Z
tags:
  - qt
  - qml
  - plan-025
  - review
  - live-verification
git:
  branch: main
  commit: 18fcc90
  worktree: /home/murphy/source/code-kb
  files:
    - docs/plans/025-qt-qml-first-class-support.md
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Qt/QML plan 025: julie wave 3 reviewed, live verification run, two fix
  rounds in flight"
type: checkpoint
next: Review B2b and julie wave 4 results, then brief B8 docs and run both
  branch gates.
---

## Qt/QML plan 025: julie wave 3 reviewed, live verification run, two fix rounds in flight

**WHAT**
- julie `feat/qt-first-class` at 723caeff (worktree `~/source/julie-extractors/.worktrees/feat-qt-first-class`): waves 1 to 3 landed tasks 1 to 18 (object model, reference contract, qmldir export, Qt JS directives, fixtures, registry specs, capability rows, ledger `## 3.2.0`, epoch `.qt-qml-v1`, docs). Lead review of lanes E, F and wave 3: no findings.
- code-kb `feat/v1.5-qt` at 1e12ef9 (this worktree): B1 to B6 plus B3b (type-target member accesses group to one row per file with `occurrences`).
- Live verification with both branch binaries on Omarchy 49306774 and Kirigami ca7d636 after a forced index rebuild (`rm artifact.db`; the unbumped 3.1.3 version string made the guard keep old rows): skeleton Button.qml 132 to 71 lines with a nested object tree; `refs BarWidget` 12 extends; `refs Color` 53 files; `refs Style` 63 files; `lookup SpeedDial` finds `component SpeedDial: Item`; `facts pragma` 5; `refs clicked` shows owned and candidate handlers; `julie-extract check` accepts the `.pragma library` JS file and `edit-file` edits a copy; `lookup waitForW` hides the tst_ helpers.

**Defects found and dispatched**
- `refs` doubles every site that has both a relationship and an identifier (extends + type_usage at one line). Fix: code-kb B2b (workflow wf_1a1b4f17-7df).
- Alias rule resolves `QQC2.Page` (Qt module) to Kirigami's own Page.qml. Fix: same B2b brief, the alias clause skips imports whose source starts with `Qt`.
- `Behavior on color` rows render as `Behavior;`. Fix: julie wave 4 task 1 (workflow wf_61471ff5-bf0), which also adds identifier metadata to goldens, bumps the version to 3.2.0, writes release notes, runs compat-check against the downloaded 3.1.3 release binary, and refreshes the qml.md evidence numbers.

**Still open**: B8 docs (README Qt section, site, CLAUDE.md/AGENTS.md alias text, release notes v1.5.0, plan 025 verification block: skeleton is 71 lines not under 60, edit-file needs a real change), B7 pin bump after the julie 3.2.0 release (owner approval), branch gates, final worktree reconciliation. Known noise: `lookup waitForW` falls back to single-letter full-text matches (pre-existing search behavior).
