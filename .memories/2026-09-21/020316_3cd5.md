---
id: checkpoint_3cd531f8
timestamp: 2026-09-21T02:03:16.657Z
tags:
  - plan-021
  - search
  - queries.rs
  - fix-round-2
git:
  branch: main
  commit: f111672
  worktree: /home/murphy/source/code-kb
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: WHAT
---

## WHAT

Narrowed one clause of `is_test_path` in `crates/code-kb-core/src/queries.rs`: the `test_` segment rule now needs the path to end with `.py` or `.rb`. `test_path_predicate` mirrors it, replacing the single `%/test\_%` clause with `%/test\_%.py` and `%/test\_%.rb`. Every other clause is unchanged from the previous commit. `TEST_PATH_CASES` is now 34 pairs, with three new entries and one flipped.

## WHY

Review ruling. `test_*.py` and `test_*.rb` are the test-file convention in Python and Ruby; no other ecosystem uses the prefix. The wider rule hid production code, for example a julie analysis module whose path carries a `test_` segment but which is not a test.

## HOW

Red first: the flipped expectation failed on the old rule. Green: `cargo test -p code-kb-core --lib -- queries::tests` 57 passed. Ceiling all clean: `cargo test -p code-kb-core --test blast_radius_test` 10 passed, `cargo test -p code-kb-core` 15 targets ok, `cargo test -p code-kb-cli --test cli_test --test mcp_test` 45 passed, `cargo fmt --all -- --check` and `cargo clippy` exit 0.

## IMPACT

development.json 89 cases: file@1 69, file@3 84, sym@1 60, sym@3 80, not-admitted 1. Unchanged from the previous round. Baseline 69 / 83 / 61 / 81 with 1.
development-2.json 60 cases: 31 / 41 / 27 / 34, not-admitted 5. Baseline 29 / 41 / 25 / 35 with 5. This is the best of the three rounds: file@1 plus 2 and sym@1 plus 2 over the baseline.
regression.json 26 cases: 25 / 26 / 22 / 25. Baseline 25 / 26 / 22 / 26.
Both named losses are fixed and each is its own top-1 at rank 1, admitted 1. The eleven remaining worse cases are the accepted one- or two-rank cap moves; no test-file row hides a labelled answer any more.
Warm search p50 was not measured this round, per the ruling that task 5 measures it. The last reading was 22 ms against a 25 ms target, and this round removes one clause and narrows two.
