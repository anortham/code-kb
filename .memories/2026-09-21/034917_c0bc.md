---
id: checkpoint_c0bcf9ba
timestamp: 2026-09-21T03:49:17.955Z
tags:
  - search
  - rerank
  - plan-022
  - task-10
git:
  branch: main
  commit: f111672
  worktree: /home/murphy/source/code-kb
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Promoted the distinct-term scorer in crates/code-kb-core/src/queries.rs
  to be the single rerank
---

Promoted the distinct-term scorer in crates/code-kb-core/src/queries.rs to be the single rerank. Deleted the Scorer struct, WeightSource, env_parsed, all five CODE_KB_RERANK* environment reads, word_weights (candidate-sample rarity), weighted_coverage, name_tier_score, W_NAME_PARTIAL, W_NAME_ANY, W_SIGNATURE, W_DOC, and the nameless cap. TEXT_CREDIT is 1.0; DOC_COVERAGE_BYTES renamed TEXT_HEAD_BYTES and now caps signature and doc at 400 bytes. rerank_with takes (candidates, query, include_tests, idf). SearchExplain drops name_coverage, signature_coverage, doc_coverage, scorer and gains term_score and name_bonus, so score is the sum of six printed parts. explain_line prints score = terms + name tier(strength) bonus + kind + path [+ doc] [+ test]. Corpus gate gained two cases on two new synthetic files. CLAUDE.md and AGENTS.md Core Invariant 6 search-ranking sentence rewritten and byte-identical.

Verification: cargo test -p code-kb-core 15 binaries all ok, 136 lib and 21 corpus; cargo test -p code-kb-cli --test cli_test --test mcp_test 34 and 11 ok; cargo fmt --all -- --check ok; cargo clippy --workspace --all-targets -D warnings clean; cmp AGENTS.md CLAUDE.md ok.

Measurement reproduces the grid run: regression 26 of 26 file at 1 and 23 of 26 symbol at 1, development 78 and 70 of 89, development-2 40 and 34 of 60. Per-case diff against results-g-c1: 1 difference in 26 regression cases (concept-discover symbol 3 to 1, the label ruling the lead added after that run), 0 differences in 89 development cases, 0 in 60 development-2 cases. p50 on the code-kb checkout 18.8 ms, budget 25 ms.

One CLI test assertion changed although the CLI crate was out of scope: cli_test.rs read explain doc_coverage, a field this task removes, and that test is in the verification scope.
