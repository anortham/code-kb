---
id: checkpoint_a0260a95
timestamp: 2026-09-21T02:55:36.582Z
tags:
  - plan-021
  - search-v1.3
  - test-path-rule
  - task-2-fix3
git:
  branch: main
  commit: f111672
  worktree: /home/murphy/source/code-kb
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Task 2 fix round 3: the test-file name rules now read the file name only"
---

Task 2 fix round 3: the test-file name rules now read the file name only. is_test_path splits the path into the directory part and the file name; directory rules read the directory part, file-name rules read the file name, and the Tests.cs rule is case-sensitive on the raw file name. test_path_predicate mirrors it in SQL with rtrim/replace for the split and GLOB for the case-sensitive rule. TEST_PATH_CASES grew from 34 to 44 paths; no existing case flipped. Found and fixed a timing defect: the unguarded SQL split cost 8.87s vs 1.27s over 548,580 hermes-agent rows, so a cheap (lower(path) LIKE '%test%' OR '%spec%') guard now runs in front of the rules; 1.83s and search p50 back at baseline. Measurement: development 70/84/61/80, development-2 31/41/27/34, regression 25/26/22/25, all identical to the t3 baseline, no case worse. Plan 021 and the v1.3.0 release notes state the new rule; CLAUDE.md and AGENTS.md needed no change. Verified: cargo test -p code-kb-core, cargo test -p code-kb-cli --test cli_test --test mcp_test, fmt, clippy -D warnings, cmp AGENTS.md CLAUDE.md, all green.
