---
id: checkpoint_083e13d1
timestamp: 2026-09-20T23:27:43.101Z
tags:
  - codex
  - review
  - search-v1.2
  - reconcile
git:
  branch: main
  commit: 4b51f19
  files:
    - crates/code-kb-core/src/queries.rs
    - crates/code-kb-core/src/sync.rs
    - crates/code-kb-core/src/workspace.rs
    - crates/code-kb-core/tests/freshness_test.rs
    - docs/plans/019-search-quality-comparison.md
    - docs/plans/020-search-v1.2-recall-and-rerank.md
    - docs/release-notes/v1.2.0.md
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Codex round 2 on the uncommitted fixes: two findings, both fixed"
briefId: search-v1-2-name-recall-and-deterministic-rerank-p
type: checkpoint
symbols:
  - collect_search_candidates
  - reconcile_offline_edits
  - remember_skipped_files
next: "User decides: commit, then push, tag v1.2.0, publish crates."
---

## Codex round 2 on the uncommitted fixes: two findings, both fixed

**WHAT:** Second Codex review (working-tree diff, 7 files) returned two verified findings, fixed with regression tests:
- **high:** `remember_skipped_files` received every added and modified path, so a file whose extractor update failed transiently (unreadable, crash) was recorded as skipped and never retried. Now only paths whose `update_file` returned Ok are remembered. Test: `test_reconcile_offline_edits_continues_when_individual_update_fails` now reconciles again after restoring permissions and asserts the file is indexed.
- **medium:** the AND pass and the OR pass each spent a full `word_cap`, so the word branch could admit 2x the documented 40-160 rows. Now a `word_admitted` set shares one cap across both passes. Test: `the_or_pass_fills_the_word_cap_but_never_exceeds_it`.

**Final measurements (release build):** regression set sym@1 22/26 (`ho-outline` 2 -> 1), everything else equal to the branch; development set all(89) 69 / 83 / 61 / 81 (file@1, file@3, sym@1, sym@3), equal or better than the branch on every aggregate; vs branch 3 cases better, 1 worse (`ju-acronym-csr` 1 -> 2). Three-provider held-out: code-kb 10/10 file@1 and sym@1 (Miller 10 / 9, Julie 6 / 2). Warm search 17.5 ms; hermes-agent per-command p50 about 700 -> 216 ms from the skipped_files change.

**Verification:** fmt, clippy -D warnings, `cargo test -p code-kb-core` and `-p code-kb-cli` all green.

**Campaign:** standalone external, budget 1/1 used, round 2/2, closed clean. Everything still uncommitted on main (7 files + 3 checkpoint files).
