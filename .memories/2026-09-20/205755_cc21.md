---
id: checkpoint_cc2172a0
timestamp: 2026-09-20T20:57:55.842Z
tags:
  - plan-020
  - search
  - rerank
  - task-5
  - fix-round-3
git:
  branch: main
  commit: a42bd4d
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Plan 020 task 5, fix round 3: word rarity sampled from the word branch only"
type: checkpoint
evidence:
  - "cargo test --workspace: 21 targets, 305 passed, 0 failed"
  - "cargo test -p code-kb-core queries::: 51 passed"
  - corpus gate 12 passed
  - clippy and fmt clean
  - development set 69/61; regression set 25/21
symbols:
  - word_weights
  - rerank
next: Task 6 official runs.
---

## Plan 020 task 5, fix round 3: word rarity sampled from the word branch only

Branch `feat/search-v1.2-recall-rerank` (worktree `.worktrees/feat-search-v1.2`), on top of 31b37e0.

- **WHAT:** `word_weights` now counts `df` over the candidates admitted by the word branch (`word_match`), the porter index's own matched set, and falls back to every candidate when no word row exists. Weights still apply to every candidate. `word_weights` takes `&[&Hits]`; `rerank` builds the sample once.
- **WHY:** the name branch admits rows precisely for containing a query word and prefers rarer substrings, so it inflated `df` for the rare words.
- **HOW:** kept under the adoption rule: development set 69/89 file@1, 61/89 sym@1 (was 68/60). Regression set unchanged at 25/26 file@1, 21/26 sym@1; `concept-fts` still file 1 -> 2 because the word branch's 40 rows for that query also mostly contain `fts` (weights create 1.79 vs fts 1.35), and `concept-telemetry` per the lead ruling.
- **IMPACT:** per-case moves: `ha-concept-docker` 2 -> 1, `ha-concept-coerce-args` 5 -> 4, `ha-concept-strip-ansi` 2 -> 8 (already a miss), `mi-concept-near-duplicate` sym 2 -> 3, `mi-concept-pack-budget` sym 2 -> 6.
