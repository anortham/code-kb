---
id: checkpoint_ddf927bf
timestamp: 2026-09-20T18:28:08.160Z
tags:
  - release
  - v1.1.4
  - crates-io
  - arm64
  - search-quality
  - plan-020
git:
  branch: main
  commit: 70e051c
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: v1.1.4 released; v1.2 search plan written
briefId: code-kb-lightweight-code-intelligence-engine-mcp-s
type: checkpoint
next: Start plan 020 task 1 (synthetic multi-language corpus test) when the user
  says go.
---

## v1.1.4 released; v1.2 search plan written

### WHAT
- Three slices committed on `main` (231f8db bug-report + /report-issue, c02f16e julie 3.1.3 + ARM64 targets, 0e785a4 search ranking + plan 019), then `chore: release v1.1.4` (08f45e8). CI green on all six jobs including the first runs of `ubuntu-24.04-arm` and `windows-11-arm`. Preflight 8/8 OK (win-test guest included).
- Tag v1.1.4 pushed; release-binaries built six archives and published the GitHub Release. Checksums recorded in `docs/release-notes/v1.1.4.md` (commit 70e051c) and synced with `gh release edit`. Linux x86_64 archive verified: sha256 OK, contents code-kb, julie-extract, README, both licenses.
- crates.io: code-kb-core 1.1.4 then code-kb-cli 1.1.4 published; `cargo install code-kb-cli --locked` from the registry reports 1.1.4. cargo-binstall is not installed on this machine, so that path was not verified.
- `docs/plans/020-search-v1.2-recall-and-rerank.md`: trigram FTS5 table on names (works inside julie-extract's process, verified on bundled SQLite 3.51), one OR recall query plus a Rust rerank (exact name, name coverage, kind prior, reference count, path role, language affinity), an in-repo synthetic corpus gate, bench harness outside the checkout. Acceptance: held-out file@1 >= 9/10, sym@1 >= 8/10, p50 < 25 ms, RSS flat.

### WHY
User chose: ship v1.1.4 with the reviewed query-side search fix, then plan v1.2. No embeddings, no Tantivy (too much complexity and a performance hit).

### IMPACT
- main is clean and pushed at 70e051c; tag v1.1.4 exists on GitHub. Memory files for this release still need their own commit (this checkpoint).
- Next work: plan 020 tasks in order (corpus test, trigram table + FTS_RULE bump, rerank, bench, v1.2.0).
