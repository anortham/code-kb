---
id: checkpoint_7bf0a110
timestamp: 2026-09-20T21:41:52.061Z
tags:
  - search-v1.2
  - review
  - codex
  - benchmark
  - plan-020
git:
  branch: main
  commit: 4b51f19
  files:
    - crates/code-kb-core/src/queries.rs
    - docs/plans/019-search-quality-comparison.md
    - docs/plans/020-search-v1.2-recall-and-rerank.md
    - docs/release-notes/v1.2.0.md
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Post-merge review of search v1.2 on main (uncommitted)
briefId: search-v1-2-name-recall-and-deterministic-rerank-p
type: checkpoint
symbols:
  - collect_search_candidates
  - fts_search_symbols_explained
  - path_role
  - rerank
next: "User decides: commit the review fixes on main, then push, tag v1.2.0,
  publish crates."
---

## Post-merge review of search v1.2 on main (uncommitted)

**WHAT:** Reviewed v1.1.4..main (plan 020) myself and through Codex (`codex exec`, structured output, validated). Three fixes applied to `crates/code-kb-core/src/queries.rs`, docs updated, all tests green. Nothing committed yet.

**Fixes:**
- Word branch admission: AND rows first, then the OR query fills the word cap (plan 020 section 3 specified OR; the branch ran AND and fell back to OR only when every row was documentation, so one full match hid every partial match). `is_documentation_language` removed as unused.
- `kind: "variable"` locals now join the candidate set inside `collect_search_candidates` (LIKE on name, `local_variable_predicate`) and go through the rerank; they used to be appended after the limit and could be pushed out by a trigram global.
- `path_role` splits on `\\` as well as `/` (Windows paths were never demoted).

**WHY:** Codex found the first two (medium, reproduced with in-memory SQLite); I found the third.

**HOW measured:** `~/.code-kb/search-eval/runner.py`. Regression set: identical to the branch result. Development set all(89): file@1 69->68, file@3 81->81, sym@1 61->60, sym@3 78->80, symMRR 0.78->0.79; 4 cases better (`ha-concept-strip-ansi` miss->1), 3 slipped one or two places (`ju-acronym-csr`, `ha-concept-quarantine`, `ha-concept-model-switch`). A pure OR branch (no AND pass) was worse (67/82/59/78, lost `mi-stem-canonicalizing`); an all-words tie-break had zero effect and was reverted.

**Three-provider benchmark (plan 019 sets, same day):** tuning(16) code-kb 15/16 file@1, 12/16 sym@1 vs julie 9 / 3 and miller 10 / 8; held-out(10) code-kb 10 / 9, julie 6 / 2, miller 10 / 9. code-kb 24 ms warm vs julie ~480 ms and miller ~280 ms. Tables recorded in plan 019 "Rerun on main after review".

**Discovery:** Julie and Miller wrote `.julie/` and `.miller/` inside the checkout; the extractor cannot index those files, so every `code-kb` command re-detected them as added and latency read 65 ms. Removed them (moved to the session scratchpad); warm median back to 24.2 ms. Files the extractor cannot index are re-detected on every start: follow-up.

**IMPACT / next:** main has 4 modified files uncommitted (queries.rs, plans 019 and 020, release notes v1.2.0). Push, tag v1.2.0, and crates publish still need the user's approval.
