---
id: checkpoint_7ffc8ff8
timestamp: 2026-09-20T11:50:09.359Z
tags:
  - release
  - workflow
  - regression-test
git:
  branch: main
  commit: 49af5eb
  files:
    - .github/workflows/release-binaries.yml
    - crates/code-kb-cli/src/mcp/protocol.rs
    - crates/code-kb-cli/src/mcp/server.rs
    - crates/code-kb-core/src/formatters.rs
    - crates/code-kb-core/src/queries.rs
    - crates/code-kb-core/src/telemetry.rs
    - docs/2026-09-20-health-review.md
    - docs/RELEASING.md
    - docs/site/index.html
    - scripts/__pycache__/benchmark_quality.cpython-314.pyc
    - scripts/benchmark_quality.py
    - tests/plugin/plugin-manifests.test.cjs
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Verified release gate with mocked external state
briefId: code-kb-lightweight-code-intelligence-engine-mcp-s
type: checkpoint
evidence:
  - "`node --test tests/plugin/plugin-manifests.test.cjs` passed (5/5)."
  - "`node --test tests/plugin/*.test.cjs` passed (16/16)."
  - Workflow YAML parses and `git diff --check` passed.
next: Integrate the workflow, docs, and plugin manifest test changes with the
  health-audit branch.
confidence: 5
---

## Verified release gate with mocked external state

### WHAT

Added `the release gate requires green exact-commit CI and matching versions` to `tests/plugin/plugin-manifests.test.cjs`.

### WHY

The workflow gate calls GitHub CLI and peels annotated tags, so YAML validation alone could not prove its behavior.

### HOW

The test extracts the gate's actual Bash block from `.github/workflows/release-binaries.yml`, then runs it against temporary mocked `git`, `gh`, and `jq` executables. It proves a green exact peeled commit succeeds and that failed, pending, missing CI, dispatch/tag mismatches, and marketplace manifest mismatch fail.

### IMPACT

Release-policy behavior is now regression-tested locally without live GitHub mutations.
