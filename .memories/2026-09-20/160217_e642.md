---
id: checkpoint_e64274dc
timestamp: 2026-09-20T16:02:17.980Z
tags:
  - review-remediation
  - telemetry
  - benchmarks
  - mcp
git:
  branch: main
  commit: 4db2800
  files:
    - README.md
    - crates/code-kb-cli/src/mcp/server.rs
    - crates/code-kb-core/src/formatters.rs
    - scripts/benchmark_quality.py
actor:
  harness: antigravity-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: WHAT
briefId: code-kb-lightweight-code-intelligence-engine-mcp-s
type: fix
symbols:
  - result_limit
  - format_references
  - measure_persistent_mcp
---

### WHAT
Addressed all accepted findings from cross-model review (Codex and Claude):
1. `scripts/benchmark_quality.py`:
   - Gated all CLI benchmark loops (`skeleton`, `slice`, and `query_benchmarks`) inside `if not args.skip_cli:`.
   - Isolated telemetry by setting `CODE_KB_TELEMETRY_DIR` to a temporary directory with `atexit` cleanup, preventing benchmarks from polluting `~/.code-kb/telemetry.db`.
   - Updated percentile formula to `math.ceil(n * pct) - 1`, matching Rust's nearest-rank algorithm.
   - Refactored changed-file reconciliation probe to use a unique disposable file and symbol with bounded polling and explicit non-absence checks.
   - Removed unused variable `scan_start`.
2. `crates/code-kb-cli/src/mcp/server.rs`:
   - Updated `result_limit` to accept string-formatted numbers.
   - Refactored `find_structural_facts` category listing into an `if/else` flow eliminating early returns so results always receive phase timing assignments.
3. `crates/code-kb-core/src/formatters.rs`:
   - Attached `format_references` doc comment to `format_references` instead of private helper `cap_notice`.
4. `README.md`:
   - Updated launcher description to describe `process.execve` on POSIX Node 22+.

### WHY
Remediate code review findings from `/codex-cli` and `/claude-cli` to ensure benchmark safety, clean telemetry isolation, accurate percentile statistics, and zero-friction limit parsing.

### HOW
Applied in-place fixes and verified with the full test suite (`cargo clippy`, `cargo fmt`, `cargo test --workspace --locked`, and `node --test tests/plugin/*.test.cjs`).

### IMPACT
Closes all review findings with zero compiler warnings and 100% test pass rate across all 19 cargo targets and 16 plugin tests.
