---
id: checkpoint_d251b20c
timestamp: 2026-09-20T16:35:56.595Z
tags:
  - bug-report
  - report-issue
  - codex-review
  - review-remediation
  - privacy
git:
  branch: main
  commit: 03d6a4c
  files:
    - .claude-plugin/skills/code-kb/SKILL.md
    - .claude-plugin/skills/report-issue/SKILL.md
    - .github/ISSUE_TEMPLATE/bug_report.md
    - README.md
    - crates/code-kb-cli/src/logging.rs
    - crates/code-kb-cli/src/main.rs
    - crates/code-kb-core/src/lib.rs
    - crates/code-kb-core/src/telemetry.rs
    - crates/code-kb-core/src/workspace.rs
    - skills/code-kb/SKILL.md
    - skills/report-issue/SKILL.md
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Codex review of the issue-reporting work: 7 findings, all fixed"
briefId: code-kb-lightweight-code-intelligence-engine-mcp-s
type: checkpoint
symbols:
  - generate_bug_report
  - mask_home_paths
  - home_candidates
  - log_tail
  - log_files_newest_first
  - build_issue_url
  - MAX_ISSUE_URL_LEN
next: Commit when the user asks. Then Windows ARM for code-kb after julie v3.1.2
  ships.
---

## Codex review of the issue-reporting work: 7 findings, all fixed

### WHAT
- `mask_home_paths` split out of `sanitize_error_message`: masks `~` only, keeps newlines. The description now runs through it; log lines use it instead of the table sanitizer.
- `workspace::log_files_newest_first` only returns `code-kb.log.*` files, newest first. `log_tail` reads backwards across rolled files until it has N lines, so running `bug-report` today no longer hides yesterday's failure log. Stray files in `.code-kb/logs` are ignored.
- `MAX_ISSUE_URL_LEN` (8000): when the pre-filled URL would exceed it, the URL body falls back to Environment + Index plus a paste note. `markdown_body` stays complete.
- `/report-issue` skill reordered: collect reproduction up front, write `markdown_body` to a scratch file, show the whole file, wait for approval, edit until approved, then `gh issue create --body-file` or hand the user the bare new-issue URL plus the file. The skill no longer uses the bundle's `github_issue_url`, because edits happen after it is built.
- Test extended: rollover across two log files, unrelated newer file ignored, home path in description masked, oversized description keeps the URL bounded.

### WHY
Codex (structured review, validated) flagged: privacy edits not reaching the fallback URL, partial privacy review, description bypassing masking, unbounded URL, unrelated log files, rollover hiding the failure log, and a fallback referencing an unwritten file. All confirmed valid.

### IMPACT
- 251 cargo tests pass, Clippy clean, fmt clean, 16 plugin tests, skill copies in sync. Dry run: stray log ignored, `/home/...` masked in the description, URL 1.8 KB.
- Still uncommitted on `main`. Julie v3.1.2 not yet released.
