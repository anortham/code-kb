---
id: checkpoint_2960ce73
timestamp: 2026-09-20T12:20:12.849Z
tags:
  - health-review
  - validation
  - bounded-output
  - telemetry
  - release-gate
  - memory
git:
  branch: main
  commit: "6e00891"
  files:
    - crates/code-kb-core/src/formatters.rs
    - docs/2026-09-20-health-review.md
    - docs/RELEASING.md
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Validated the 2026-09-20 health review and its repairs
briefId: code-kb-lightweight-code-intelligence-engine-mcp-s
type: checkpoint
evidence:
  - "cargo test --workspace --locked: 247 passed, 0 failed, 19 targets"
  - "node --test tests/plugin/*.test.cjs: 16 passed"
  - "cargo clippy --workspace --all-targets -- -D warnings: clean"
  - "cargo fmt --all -- --check: clean"
  - "/proc/<pid>/smaps_rollup on target/release/code-kb serve after 7 lookups:
    Pss 22241-22907 kB, Pss_Anon 17032-17696 kB"
next: "User decides: commit and push, then whether to lower memory or restate
  the 15 MB claim, and which of the four follow-up candidates to plan."
---

## Validated the 2026-09-20 health review and its repairs

**WHAT:** Reviewed `docs/2026-09-20-health-review.md` and the unpushed commit `6e00891` (bounded 0–200 limits, qualified lookup filters, shared fact/literal limit, logical telemetry counts, cap notices, release CI gate, benchmark labels). Made two small follow-up edits and rebuilt the release binary.

**WHY:** The user asked for an independent check of another agent's review and local changes before deciding what to do next.

**HOW:**
- Re-ran the full gate: 247 Rust tests / 19 targets, 16 Node plugin tests, clippy, fmt, AGENTS/CLAUDE sync. All pass.
- Re-derived every telemetry number from `~/.code-kb/telemetry.db`; all match the doc (p99 values differ only by rank method).
- Confirmed every internal caller of the bounded query functions uses a limit of 50 or less, so the 200 ceiling breaks nothing.
- Confirmed CI runs on push to main, so the release gate's exact-commit lookup works for tagged main commits.
- Edit 1: `formatters.rs` cap notices now share `cap_notice()`; at limit 200 the notice says "narrow the query" instead of "increase limit".
- Edit 2: `docs/RELEASING.md` gained a recovery step for a failed release gate (re-run the workflow on the existing tag with `--ref`).
- Rebuilt `target/release`, so the harness symlink now runs v1.1.3 instead of the stale v1.1.1.
- Measured server memory on the real repo index: PSS 22.2–22.9 MiB, anonymous 17.0–17.7 MiB, RSS 24.6–25.2 MiB. Higher than the doc's fixture numbers; even heap-only exceeds the 15 MB claim.

**IMPACT:** The review and its repairs are sound. Commit `6e00891` is still unpushed, and the two edits plus the doc's validation section are uncommitted. Open product decision: the "<15 MB retained memory" claim in README and CLAUDE.md is not met on a real repo. Follow-up candidates from the doc (launcher memory, version-aware telemetry percentiles, persistent-MCP benchmark, cross-language answer-quality corpus) are not yet planned.
