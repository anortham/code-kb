---
id: checkpoint_267073c7
timestamp: 2026-09-20T20:53:05.813Z
tags:
  - plan-020
  - search
  - rerank
  - task-5
  - fix-round-2
git:
  branch: main
  commit: a42bd4d
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Plan 020 task 5, fix round 2: token-level signature and doc coverage"
type: checkpoint
evidence:
  - "cargo test --workspace: 21 targets, 305 passed, 0 failed"
  - "cargo test -p code-kb-core queries::: 51 passed"
  - "cargo test -p code-kb-core --test search_corpus_test: 12 passed"
  - "cargo clippy --workspace --all-targets -- -D warnings: clean"
  - "cargo fmt --check: clean"
  - development set 68/60; regression set 25/21
symbols:
  - text_hits
  - text_tokens_into
  - split_identifier_into
  - identifier_boundary
  - lowercase_prefix_match
  - rerank
next: Task 6 official runs; lead decision on concept-fts (within-set rarity
  favors `create` over `fts` because the name branch admits many `*fts*` names).
---

## Plan 020 task 5, fix round 2: token-level signature and doc coverage

Branch `feat/search-v1.2-recall-rerank` (worktree `.worktrees/feat-search-v1.2`), on top of 04a4f38.

- **WHAT:** `text_hits` no longer uses substring containment. The signature and the 400-byte doc are tokenized like names (non-word split, then the identifier split rule) and a query word is covered when a token equals it, or starts with its stem (3+ chars) or the word (3+ chars). `stemming` covers `stemmer`/`stems` but not `system`; `porter` no longer matches `important`. The identifier split rule now lives once in `identifier_boundary` over character classes; `split_identifier_into` (char path) and `text_tokens_into` (one-pass ASCII byte path, char fallback) share it. One token buffer is reused across candidates; ASCII prefix compares avoid allocation.
- **WHY:** reviewer finding on 04a4f38: substring stems inflated doc coverage (`stem` in `system` put `create_index` above `ensure_fts_index` on the regression query).
- **HOW:** weights unchanged after a re-sweep (doc 14 -> 67/59, doc 18 -> 68/60, doc 22 -> 66/58, sig 8 -> 68/60 with lower sym@3): sig 4, doc 18. Timer A/B: no text work ~0.35 ms, tokenize ~+0.2 ms, match ~+0.1 ms at ~170 candidates.
- **IMPACT:** development set 68/89 file@1, 60/89 sym@1 (unchanged totals, `mi-concept-disk-preflight` sym 5 -> 3). Regression set 25/26 file@1, 21/26 sym@1; still worse than v1.1.4: `concept-fts` (file 1 -> 2, now because `create` is rarer than `fts` inside the admitted set: df 7 vs 14, so `create_index` gets 7.1 name points to `ensure_fts_index`'s 5.9) and `concept-telemetry` (whole-name struct; lead ruled no change). Rerank timer 0.44-0.75 ms at 160-177 candidates (min of 5); warm search median 24.3 ms.
