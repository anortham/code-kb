---
id: checkpoint_00845915
timestamp: 2026-09-18T19:09:49.154Z
tags:
  - plan-018
  - codex-review
  - fts
git:
  branch: fix/julie-data-usage
  commit: 683a988
  files:
    - crates/code-kb-core/src/db.rs
    - crates/code-kb-core/src/queries.rs
    - crates/code-kb-core/tests/blast_radius_test.rs
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Codex review of plan 018 branches: three fixes applied"
briefId: code-kb-lightweight-code-intelligence-engine-mcp-s
type: checkpoint
---

## Codex review of plan 018 branches: three fixes applied

**WHAT:** An external codex review (structured, validated) of `fix/julie-data-usage` and the julie branch returned four medium findings and nothing on julie. Three were fixed on this branch:
- `find_related_tests` pending query now uses `SELECT DISTINCT`, so one test with many calls no longer fills the limit and hides other tests.
- The blast-radius stem-matched test-file fallback skips files whose symbols are documentation.
- The `symbols_ai` trigger also removes same-file variables that became local when their enclosing function is inserted after them (julie writes children before parents in spooled updates). The old test that accepted that drift now requires removal.

**Accepted limitation:** with `kind = "variable"`, locals match by name only because they are not in the FTS index. Documented on `search_symbols_scoped`.

**Verified:** full `cargo test --workspace`, clippy, fmt, live `context scan_workspace`, and a real JIT update with a new local that stays out of the FTS index.

**IMPACT:** Both branches remain unpushed. Next: push and merge, release julie 3.1.0, bump the pin.
