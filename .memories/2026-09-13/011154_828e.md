---
id: checkpoint_828ea8b8
timestamp: 2026-09-13T01:11:54.998Z
tags:
  - review
  - context-slice
  - query
git:
  branch: main
  commit: fac0c7f
  files:
    - .gitignore
    - Cargo.lock
    - README.md
    - TODO.md
    - crates/code-kb-cli/Cargo.toml
    - crates/code-kb-cli/src/mcp/server.rs
    - crates/code-kb-cli/tests/mcp_test.rs
    - crates/code-kb-core/src/ops.rs
    - crates/code-kb-core/src/queries.rs
    - crates/code-kb-core/src/sync.rs
    - crates/code-kb-core/src/watcher.rs
    - crates/code-kb-core/src/workspace.rs
    - crates/code-kb-core/tests/freshness_test.rs
    - crates/code-kb-core/tests/watcher_test.rs
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: WHAT
briefId: code-kb-lightweight-code-intelligence-engine-mcp-s
type: checkpoint
symbols:
  - get_context_slice_op
  - find_references_for_symbol
---

## WHAT

Completed the approved qualified-context correction: `get_context_slice_op` now requests callees using the resolved target symbol ID. Escaped category matching in structural-fact and literal queries.

## WHY

Qualified requests such as `McpServer::new` returned no dependencies because relationships index terminal symbol names, while an unqualified lookup merged unrelated `new` methods.

## HOW

Added a private ID-filtered reference helper that applies the source ID in relationship and pending-relationship queries before SQL LIMIT. Context slices use the target’s terminal name and ID; related tests use the resolved terminal name. Added a regression fixture with two `new` methods and different dependencies.

## IMPACT

Context dependencies now correspond to the selected symbol rather than every same-named method. Core library unit suite has 29 passing tests with `/home/murphy/source/julie-extractors/target/release` prepended to PATH.
