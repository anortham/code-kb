---
id: checkpoint_d8ccf1a0
timestamp: 2026-09-13T01:07:22.926Z
tags:
  - review
  - packaging
  - mcp
git:
  branch: main
  commit: fac0c7f
  files:
    - README.md
    - crates/code-kb-cli/Cargo.toml
    - crates/code-kb-core/src/ops.rs
    - crates/code-kb-core/src/queries.rs
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: CLI/MCP packaging review
briefId: code-kb-lightweight-code-intelligence-engine-mcp-s
type: checkpoint
evidence:
  - cargo test -p code-kb-cli --test mcp_test test_mcp_stdio_handshake_and_tools
    -- --exact passed
  - cargo package -p code-kb-core --allow-dirty --no-verify passed
  - CLI cargo package now fails only resolving unpublished code-kb-core from
    crates.io
next: Lead to decide release workflow, publication sequencing, and whether
  concise MCP server instructions are worth adding.
---

## CLI/MCP packaging review

### WHAT

- Reviewed CLI manifests, stdio MCP server, README installation claims, plugin manifest, and release configuration.
- Updated `crates/code-kb-cli/Cargo.toml` so the publishable CLI dependency on `code-kb-core` has version `0.1.0`.
- Updated README Rust prerequisite from 1.85 to the workspace MSRV, 1.95.

### WHY

`cargo package -p code-kb-cli --allow-dirty --no-verify` previously failed before packaging because a path dependency lacked a version requirement. The README advertised a Rust version below the manifest requirement.

### HOW

- Confirmed valid MCP JSON field casing with a live stdio request and focused MCP test; rejected the initial suspected casing issue.
- Confirmed core packages successfully. CLI package preparation now resolves its versioned dependency and fails only because `code-kb-core 0.1.0` is not yet published to crates.io.
- Found no GitHub Actions or release workflow in the repository. Server instructions are absent; this is an optional adoption improvement, not a protocol compliance defect.

### IMPACT

Publishing requires `code-kb-core` to be published first, followed by CLI packaging/publishing. A release workflow must be added before promising Apple Intel/ARM, Windows, and Linux release assets. `julie-extract` remains an external PATH prerequisite and is not bundled or installed by this package.
