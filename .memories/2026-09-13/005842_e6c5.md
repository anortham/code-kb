---
id: checkpoint_e6c59893
timestamp: 2026-09-13T00:58:42.017Z
tags:
  - review-followup
  - tree-sitter
  - concurrency
  - memory-bounds
  - safety
git:
  branch: main
  commit: cc85353
  files:
    - .codex/config.toml
    - Cargo.toml
    - README.md
    - TODO.md
    - crates/code-kb-cli/Cargo.toml
    - crates/code-kb-cli/src/main.rs
    - crates/code-kb-cli/src/mcp/server.rs
    - crates/code-kb-core/Cargo.toml
    - crates/code-kb-core/src/edit.rs
    - crates/code-kb-core/src/formatters.rs
    - crates/code-kb-core/src/lib.rs
    - crates/code-kb-core/src/ops.rs
    - crates/code-kb-core/src/queries.rs
    - crates/code-kb-core/src/slicer.rs
    - crates/code-kb-core/src/sync.rs
    - crates/code-kb-core/src/syntax.rs
    - crates/code-kb-core/src/workspace.rs
    - crates/code-kb-core/tests/disambiguation_test.rs
    - crates/code-kb-core/tests/edit_test.rs
    - crates/code-kb-core/tests/freshness_test.rs
    - crates/code-kb-core/tests/watcher_test.rs
actor:
  harness: antigravity-client
  user: alann
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Astra Review Follow-Up Hardening
briefId: code-kb-lightweight-code-intelligence-engine-mcp-s
type: checkpoint
context: Astra second review follow-up findings across code-kb-core and code-kb-cli
decision: Implement language-specific Tree-Sitter validation, strict edit
  concurrency and pre-commit disk checks, atomic rollback with verification
  guards, exact path isolation in queries, and memory-bounded outline &
  streaming cold-start reconciliation.
impact: Full review compliance, AST-accurate validation, safe concurrency &
  atomic rollbacks, isolated path queries, and strict O(1) heap memory bounds.
evidence:
  - "cargo test --all: 42 passed, 0 failed"
  - "cargo clippy --all-targets -- -D warnings: 0 warnings"
  - "fc.exe /b AGENTS.md CLAUDE.md: 0 diff"
symbols:
  - validate_syntax
  - replace_symbol_body
  - load_file_symbols
  - codebase_outline_op
  - load_scoped_outline_symbols
  - reconcile_offline_edits
next: Commit, push to origin/main, pull down on Linux to test platform parity.
confidence: 5
---

## Astra Review Follow-Up Hardening

Resolved all 5 follow-up findings from Astra review:
- **Finding 1 (Syntax Validation):** Replaced delimiter counter with multi-language Tree-Sitter validation (`tree-sitter`, `tree-sitter-rust`, `tree-sitter-javascript`, `tree-sitter-typescript`, `tree-sitter-python`, `tree-sitter-go`). Passes lifetimes and raw strings; rejects invalid JavaScript.
- **Finding 2 (Edit Concurrency & Freshness):** Propagate refresh errors, verify `expected_body_hash` against actual disk content (and verify `files.content_hash` before accepting indexed hashes), and check disk bytes right before persistence.
- **Finding 3 (Atomic Rollback):** Check disk content before rollback to avoid clobbering concurrent modifications; use atomic `tempfile_in` + `persist` rollback replacement; report `SyncRollbackFailed` if rollback fails.
- **Finding 4 (File Skeletons Isolation):** Removed suffix matching in `load_file_symbols` and use exact normalized path matches so `file_skeleton('src/lib.rs')` never matches `nested/src/lib.rs`.
- **Finding 5 (Memory Bounds):** Added SQLite depth pushdown in `load_scoped_outline_symbols`, streamed outline paths row-by-row into pruned `OutlineNode`, and replaced repository-wide HashMaps in `reconcile_offline_edits` with in-memory SQLite table and streaming statements.

All 42 tests pass across workspace and strict clippy passes with zero warnings.
