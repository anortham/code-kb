---
id: checkpoint_ec7234b6
timestamp: 2026-09-13T01:17:01.582Z
tags:
  - review
  - bug-fix
  - release-readiness
  - freshness
  - mcp
  - packaging
git:
  branch: main
  commit: fac0c7f
  files:
    - .gitignore
    - Cargo.lock
    - Cargo.toml
    - README.md
    - TODO.md
    - crates/code-kb-cli/Cargo.toml
    - crates/code-kb-cli/src/logging.rs
    - crates/code-kb-cli/src/main.rs
    - crates/code-kb-cli/src/mcp/protocol.rs
    - crates/code-kb-cli/src/mcp/server.rs
    - crates/code-kb-cli/tests/mcp_test.rs
    - crates/code-kb-core/src/db.rs
    - crates/code-kb-core/src/edit.rs
    - crates/code-kb-core/src/formatters.rs
    - crates/code-kb-core/src/lib.rs
    - crates/code-kb-core/src/models.rs
    - crates/code-kb-core/src/ops.rs
    - crates/code-kb-core/src/queries.rs
    - crates/code-kb-core/src/slicer.rs
    - crates/code-kb-core/src/sync.rs
    - crates/code-kb-core/src/syntax.rs
    - crates/code-kb-core/src/watcher.rs
    - crates/code-kb-core/src/workspace.rs
    - crates/code-kb-core/tests/disambiguation_test.rs
    - crates/code-kb-core/tests/edit_test.rs
    - crates/code-kb-core/tests/freshness_test.rs
    - crates/code-kb-core/tests/watcher_test.rs
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: WHAT
briefId: code-kb-lightweight-code-intelligence-engine-mcp-s
evidence:
  - "cargo test --workspace --all-targets --locked: 51 passed with julie-extract
    on PATH"
  - "cargo test -p code-kb-core reconcile_offline_edits after lint fix: 4 passed"
  - "cargo clippy --workspace --all-targets --locked -- -D warnings: pass"
  - "cargo fmt --all -- --check; git diff --check; cmp AGENTS.md CLAUDE.md: pass"
  - "cargo package -p code-kb-core --allow-dirty --locked: packaged and verified"
symbols:
  - codebase_outline_op
  - get_context_slice_op
  - ensure_fresh_file
  - reconcile_offline_edits
  - start_watcher
next: Choose license policy and extractor distribution strategy; implement
  native CI/release jobs after those choices. Decide bounded-output semantics
  before claiming memory target.
---

## WHAT
Reviewed the new project and answered all four TODO.md questions. Fixed absolute outline paths, literal LIKE matching, qualified context-slice dependency identity, stale deleted-file records, unreadable-file freshness, reconciliation error propagation and traversal safety, watcher prefix/ignore handling, and native-platform test expectations. Added MCP initialize guidance/version reporting and ignored unknown notifications; corrected package metadata and README claims, included Cargo.lock, and formatted the workspace.

## WHY
Agents could receive empty, stale, or unrelated context. Reconciliation could delete indexed records after an incomplete walk or hide extractor failures. Packaging metadata and unsupported performance claims overstated readiness.

## HOW
Terra workers inspected separate scopes; lead verified findings and changes. Reused workspace resolution, SQLite queries, tempfile-backed reconciliation tracking, and ignore 0.4.33 incremental matchers. Added focused regressions with the real julie-extract 2.42.0 binary. Shared CLI/core interfaces stayed intact; no new tool parameters or output caps.

## IMPACT
Linux workspace tests passed 51/51; after a behavior-preserving lint correction, 4 reconciliation tests passed. Strict clippy, fmt, byte-identical AGENTS/CLAUDE, diff checks, and current core package verification pass. Release remains unready: core must publish before CLI, extractor distribution and four native builds are absent, license metadata disagrees and license files are missing. Outline width and ReconcileReport grow with file counts; the <15MB target is unproved. Standard MCP roots/list remains unsupported. TODO.md records recommendations; hooks/skills deferred pending dogfooding. No release or push authorized.
