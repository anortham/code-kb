---
id: checkpoint_411d771f
timestamp: 2026-09-13T21:51:08.273Z
tags:
  - phase5
  - tests
  - hardening
  - regression
git:
  branch: fix/phase1-server-stability
  commit: a4af8dd
  files:
    - crates/code-kb-cli/tests/cli_test.rs
    - crates/code-kb-cli/tests/mcp_test.rs
    - crates/code-kb-core/src/formatters.rs
    - crates/code-kb-core/src/sync.rs
    - crates/code-kb-core/tests/disambiguation_test.rs
    - crates/code-kb-core/tests/edit_test.rs
    - crates/code-kb-core/tests/freshness_test.rs
    - crates/code-kb-core/tests/watcher_test.rs
    - crates/code-kb-core/tests/worktree_test.rs
actor:
  harness: antigravity-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Phase 5: Test Hardening (Items 25–27)"
briefId: code-kb-lightweight-code-intelligence-engine-mcp-s
type: checkpoint
decision: "Completed Phase 5 test hardening: enforced julie-extract binary
  presence in all integration tests, strengthened MCP assertions on isError and
  content, strengthened CLI symbol and search assertions, added chained edit
  optimistic lock tests, and verified all 83 workspace tests pass."
impact: Eliminated silent test bypasses when extractor is absent, prevented
  masked tool errors in MCP tests, and added comprehensive regression coverage
  for optimistic locking.
evidence:
  - TMPDIR=$PWD/target/tmp cargo test --workspace --locked passed (83 tests)
  - cargo clippy --workspace --all-targets -- -D warnings passed with zero
    warnings
  - cargo fmt --all -- --check passed
next: "Phase 6: Release Preparation (Item 7) - review version bump to 0.5.1 and
  discuss release cut with user."
confidence: 5
---

## Phase 5: Test Hardening (Items 25–27)

### WHAT
- Replaced silent test skipping (`if extract_bin.is_none() { return; }`) across `worktree_test.rs`, `watcher_test.rs`, `freshness_test.rs`, `edit_test.rs`, `disambiguation_test.rs`, `cli_test.rs`, and `sync.rs` with `.expect("julie-extract binary must be present for tests")`.
- Added missing `structural_facts` and `literals` tables to `mcp_test.rs` mock schema batch.
- Strengthened MCP tool assertions in `mcp_test.rs` to check `assert_ne!(resp["result"]["isError"], true)` and inspect returned content strings.
- Strengthened CLI `find_symbol` and `search_symbols` assertions in `cli_test.rs` to inspect concrete symbol signatures and test `--json` output structure.
- Added regression test `test_replace_symbol_body_chained_edits_with_expected_hash` in `edit_test.rs` validating multi-step optimistic locking with `body_hash`.

### VERIFICATION
- All 83 workspace tests pass cleanly in ~2.5s with `TMPDIR=$PWD/target/tmp cargo test --workspace --locked`.
- `cargo clippy --workspace --all-targets -- -D warnings` clean.
- `cargo fmt --all -- --check` clean.
- `cmp AGENTS.md CLAUDE.md` identical.
