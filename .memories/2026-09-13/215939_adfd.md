---
id: checkpoint_adfd319a
timestamp: 2026-09-13T21:59:39.792Z
tags:
  - release
  - v0.5.1
  - github-release
  - shipped
git:
  branch: main
  commit: 06098c5
actor:
  harness: antigravity-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Release v0.5.1 Shipped
briefId: code-kb-lightweight-code-intelligence-engine-mcp-s
type: checkpoint
decision: Merged fix/phase1-server-stability into main, tagged v0.5.1, pushed
  main and tag to origin, and verified successful GitHub Release v0.5.1 with all
  4 matrix binary archives published.
impact: v0.5.1 release is officially live for users and AI coding agents across
  Linux, Windows, and macOS, with all 27 audit and stabilization fixes included.
evidence:
  - gh release view v0.5.1 showed 8 assets published successfully
  - gh run watch 34785263430 completed with success in ~3m
  - CI run 34785263685 on main passed green
next: Optional crates.io publication (cargo publish -p code-kb-core && cargo
  publish -p code-kb-cli).
confidence: 5
---

## Release v0.5.1 Shipped

### WHAT
- Fast-forward merged `fix/phase1-server-stability` into `main`.
- Created annotated git tag `v0.5.1`.
- Pushed `main` and tag `v0.5.1` to `origin`.
- Monitored GitHub Actions `Release Binaries` workflow run `34785263430` to successful completion.
- Verified GitHub Release `v0.5.1` published with all assets:
  - Linux x86_64 (`.tar.gz` + `.sha256`)
  - macOS Apple Silicon (`.tar.gz` + `.sha256`)
  - macOS Intel (`.tar.gz` + `.sha256`)
  - Windows x86_64 (`.zip` + `.sha256`)
- Verified CI workflow run `34785263685` on `main` passed green.

### VERIFICATION
- `gh release view v0.5.1` confirms published status and all 8 assets.
- `gh run list --workflow=ci.yml` confirms CI pass on `main`.
