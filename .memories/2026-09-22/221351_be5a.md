---
id: checkpoint_be5abb8a
timestamp: 2026-09-22T22:13:51.657Z
tags:
  - read-tools
  - claude-review
  - mcp-schema
  - symbol-selection
git:
  branch: refactor/read-only-tools
  commit: 0047a09
  files:
    - crates/code-kb-cli/src/mcp/server.rs
    - crates/code-kb-cli/tests/mcp_test.rs
    - docs/decisions/002-exact-symbol-selection.md
    - docs/plans/2026-09-22-read-tool-precision.md
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: WHAT
briefId: improve-precision-in-the-ten-read-tools
type: decision
decision: Advertise optional name and ID selectors in Claude-compatible MCP
  object schemas, and enforce exactly one nonempty selector in handlers and CLI.
alternatives:
  - Keep top-level oneOf and minLength in MCP schemas; Claude tool validation
    rejects these constraints.
evidence:
  - "cargo test --workspace --locked --no-fail-fast: 394 passed, 0 failed"
  - "node --test tests/plugin/*.test.cjs: 17 passed"
  - "cargo clippy --workspace --all-targets --locked -- -D warnings: passed"
  - "cargo fmt --all -- --check: passed"
symbols:
  - McpServer::selector
  - McpServer::tool_definitions
next: Commit reviewed fixes locally, sync that clean SHA to win-test, and run
  the Windows workspace test gate.
---

## WHAT
Claude CLI reviewed the clean read-only-tools branch diff through 0047a09. Two actionable findings were fixed in crates/code-kb-cli/src/mcp/server.rs and MCP tests: remove top-level oneOf/minLength from selector schemas, and treat JSON null as absent while preserving name-alias precedence. Updated the precision plan and exact-selection decision note.

## WHY
Claude tool schema validation can reject those JSON Schema constraints. The previous selector parser also broke existing null-placeholder and duplicate-alias behavior.

## HOW
Added unit and MCP integration regressions first and observed both fail. Kept the tool properties as optional strings with explicit descriptions; handler validation now enforces exclusivity. Checked Anthropic's schema guidance and independently assessed the review's low-severity heading suggestion as cosmetic because blast-radius seeds already carry the selected ID.

## IMPACT
Linux branch gate passes: 394 Rust tests, 17 plugin tests, clippy, formatting, sync-contract comparisons, and git diff check. Windows guest run remains before final completion. No push, merge, or release.
