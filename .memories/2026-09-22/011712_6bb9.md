---
id: checkpoint_6bb9b549
timestamp: 2026-09-22T01:17:12.194Z
tags:
  - release
  - v1.5.0
  - checksums
git:
  branch: main
  commit: 12fc331
  files:
    - docs/release-notes/v1.5.0.md
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: v1.5.0 release archives verified and recorded
type: checkpoint
evidence:
  - Release run https://github.com/anortham/code-kb/actions/runs/35674799052
    success
  - release published 2026-09-22T01:16:16Z
  - six archives api-ok sidecar-ok
next: "Commit docs(release): record v1.5.0 archive checksums, push, update the
  GitHub release body, publish both crates."
---

## v1.5.0 release archives verified and recorded

**WHAT:** The Release Binaries run 35674799052 for tag `v1.5.0` (commit 12fc331) succeeded. All six archives were downloaded, hashed, and cross-checked against the GitHub API digest and the published `.sha256` sidecars. The checksum table now replaces the "Published with the release." placeholder in `docs/release-notes/v1.5.0.md`.

**WHY:** The release notes are the durable record of the archive checksums, and the crates publish must follow the GitHub release because `cargo binstall` downloads these archives.

**HOW:** `kb-record-checksums.sh` (session scratchpad) did the download, digest check, sidecar check, and notes rewrite in one pass. The Linux x86_64 archive was unpacked and run: `code-kb 1.5.0`, `julie-extract 3.2.0`, README and both licenses present. `cargo binstall` is not installed on this machine, so that download path was checked by hand.

**IMPACT:** Next: commit and push the notes, `gh release edit v1.5.0 --notes-file`, then `cargo publish -p code-kb-core --locked` and `cargo publish -p code-kb-cli --locked` (core dry-run already passed).
