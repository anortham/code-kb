---
id: checkpoint_7fda06a3
timestamp: 2026-09-22T22:34:26.398Z
tags:
  - release
  - v2.0.0
  - windows-ci
  - watcher
  - timing
git:
  branch: main
  commit: 627772c
  files:
    - crates/code-kb-core/tests/watcher_test.rs
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: WHAT
briefId: improve-precision-in-the-ten-read-tools
type: incident
context: GitHub CI run 35792504965 for release candidate 627772c failed only on
  windows-latest; four other platform jobs and lint passed.
evidence:
  - "https://github.com/anortham/code-kb/actions/runs/35792504965:
    windows-latest watcher test failed at content_hash assertion"
  - "focused watcher test: 1 passed"
  - "cargo test --workspace --locked --no-fail-fast: 394 passed"
  - "cargo clippy --workspace --all-targets --locked -- -D warnings: passed"
next: Commit the test repair, sync to Windows guest, rerun full Windows suite,
  push main, and wait for new exact-commit CI before tagging.
---

## WHAT
Adjusted crates/code-kb-core/tests/watcher_test.rs::test_watcher_refreshes_a_native_header_edit to poll for a changed indexed file hash for up to five seconds, matching the neighboring header-batch watcher test, while retaining the exact one-revision assertion.

## WHY
The Windows x64 GitHub runner failed this test because a fixed 700 ms sleep elapsed before asynchronous watcher/debouncer/extractor work completed. All other tests in that job passed, the Windows ARM job passed, and the same exact commit passed 388 tests in the local NTFS guest.

## HOW
Read the complete failed CI log and both header watcher tests. The neighboring test already uses 25 x 200 ms condition polling for the same .h refresh path. The CI failure supplied the red reproduction; the focused test, full Linux suite, fmt, and clippy pass after the narrow test-only change.

## IMPACT
The test still detects missing refresh and wrong revision counts, but no longer assumes Windows CI finishes asynchronous work within 700 ms. Release tagging remains paused until a new commit passes local Windows and exact-commit GitHub CI.
