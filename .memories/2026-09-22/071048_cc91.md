---
id: checkpoint_cc917ee5
timestamp: 2026-09-22T07:10:48.665Z
tags:
  - release
  - v1.6.0
  - qt
  - cpp
  - checksums
git:
  branch: main
  commit: 0ea35d1
  files:
    - docs/release-notes/v1.6.0.md
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "code-kb v1.6.0 released: Qt C++ headers, julie-extract 3.3.0 pin, the
  first-class Qt claim"
type: checkpoint
next: Commit the checksums, publish code-kb-cli 1.6.0, verify cargo install from
  the registry, checkpoint commit, push main.
---

## code-kb v1.6.0 released: Qt C++ headers, julie-extract 3.3.0 pin, the first-class Qt claim

**WHAT:** `feat/v1.6-qt-cpp` (six commits: pin 3.3.0 c32f501, `property` fact alias 23c7485, Qt C++ tests 12a540c, docs and claim c7d1f7d, version bump ec02099, skeleton `// event` marker 0ea35d1) fast-forwarded into main. CI run 35697280628 green on 0ea35d1 (six jobs), preflight 8/8 (Windows guest ran worktree, Qt C++, alias, and reference tests on the Windows 3.3.0 extractor), tag `v1.6.0` at 0ea35d1, Release Binaries run 35697853342 published six archives at 2026-09-22T07:09:20Z. Every archive SHA-256 matched the GitHub digest and its sidecar; the Linux x86_64 archive runs `code-kb 1.6.0` and `julie-extract 3.3.0`. `code-kb-core 1.6.0` is on crates.io; the CLI crate follows this commit.

**WHY:** Plan 025 Part C item 6: a Kirigami header (`src/layouts/columnview.h`) yields 231 skeleton lines, 38 `Q_PROPERTY` rows, 37 `// event` rows, 0 parse banners, 1 class row; the README now carries the first-class Qt claim.

**HOW:** One Opus workflow agent implemented the wave brief with serial-worker commits; the lead reviewed inline and added the event marker (`unspelled_kind` in `formatters.rs`) with red tests.

**IMPACT:** Users on the plugin or `cargo install` get julie-extract 3.3.0 bundled; the index rebuilds once. Next: publish `code-kb-cli 1.6.0`, verify `cargo install`, push main.
