---
id: checkpoint_51c084c8
timestamp: 2026-09-22T15:37:25.823Z
tags:
  - julie-3.3.1
  - qt
  - qml
git:
  branch: main
  commit: a36f2c5
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Qt/QML downstream integration
type: checkpoint
evidence:
  - Focused formatter unit passed.
  - QML directive focused test passed with julie-extract 3.3.1.
  - C++ edit/reindex focused test reproduces cpp-to-c update regression.
symbols:
  - StructuralFact
  - find_structural_facts_scoped
  - format_structural_facts
next: Await extractor_contract sync containment, then rerun qt_cpp_test suite.
---

## Qt/QML downstream integration

**WHAT** Added optional JSON metadata to `StructuralFact`, loaded `structural_facts.metadata_json`, and rendered compact Qt `Q_PROPERTY` details for property type, accessors, and 3.3.1's designable/scriptable/stored/user/revision fields. Added real-extractor C++ and JavaScript QML directive coverage.

**WHY** Julie 3.3.1 now emits useful Qt property metadata and corrected QML directive spans; code-kb previously discarded fact metadata and its `pragma` alias missed `javascript.qml_directive.v1`.

**HOW** Kept generic structural-fact output unchanged, using a Qt-only metadata renderer and display-name fallback. The QML test verifies `.import` signatures/body exclude trailing comments and `facts pragma` finds `.pragma library`. The C++ test verifies metadata and syntax validation. It also exposes a separate Julia update issue: `update` reclassifies a previously C++ `.h` as C and drops Q_PROPERTY symbols, while ordinary incremental `scan` preserves C++.

**IMPACT** Agent-facing facts now retain 3.3.1 Qt metadata. `qt_header_edits_validate_and_reindex_with_qt_macros` is intentionally red until the sync fallback owner fixes the extractor update classification issue.
