---
id: checkpoint_71fd7bea
timestamp: 2026-09-22T07:12:24.705Z
tags:
  - release
  - v1.6.0
  - crates-io
  - closeout
git:
  branch: main
  commit: 9836f6a
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "v1.6.0 release closeout: crates published, install verified, plan 025 closed"
type: checkpoint
---

## v1.6.0 release closeout: crates published, install verified, plan 025 closed

**WHAT:** `code-kb-core 1.6.0` and `code-kb-cli 1.6.0` are on crates.io (the CLI crate warns `scripts/julie-pins.json not found; bypassing julie-extract build verification`, expected in the packaged crate). `cargo install code-kb-cli --locked` from the registry into a scratch root reports `code-kb 1.6.0`. The GitHub release `v1.6.0` carries the notes with the six checksum rows. Checksums committed as 9836f6a.

**WHY:** Closes plan 025 (`docs/plans/025-qt-qml-first-class-support.md`) on both repositories: julie-extract 3.2.0 + code-kb v1.5.0 shipped the QML side, julie-extract 3.3.0 + code-kb v1.6.0 the Qt C++ side and the first-class claim.

**HOW:** Same path as v1.5.0: preflight 8/8, CI green on the tagged commit, Release Binaries, `kb-record-checksums` cross-check (GitHub digest, sidecar, unpacked Linux archive runs `code-kb 1.6.0` and `julie-extract 3.3.0`), then core before CLI on crates.io.

**IMPACT:** The local harness runs main `target/release/code-kb` 1.6.0 with `.tools/julie-extract` 3.3.0. Worktrees `feat-v1.6-qt-cpp` (code-kb) and `feat-qt-cpp-macros` (julie) are merged and clean, left in place. Open follow-up: QML-fixture tests for the reference dedupe.
