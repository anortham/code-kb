---
id: checkpoint_1fd99ab3
timestamp: 2026-09-22T06:47:47.539Z
tags:
  - v1.6.0
  - release
git:
  branch: main
  commit: e0bb85d
  worktree: /home/murphy/source/code-kb
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "K6: version bump to 1.6.0"
type: checkpoint
evidence:
  - "kb-version-bump.sh: remaining OLD mentions -> (none)"
  - node --test tests/plugin/*.test.cjs -> 17 pass 0 fail
next: "K7 gates: fmt, clippy, the workspace test run, plugin tests, the
  CLAUDE/AGENTS diff, and release-preflight.sh through step 6."
---

## K6: version bump to 1.6.0

Ran the version bump across every version site.

- **What:** `Cargo.toml`, `crates/code-kb-cli/Cargo.toml` core dependency, the four plugin manifests (`.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `.codex-plugin/plugin.json`, `plugin.json`), the release workflow default, the site badge and stylesheet query, plus `Cargo.lock`.
- **Why:** the release notes, the launcher download path, and the crate version must all name one version.
- **How:** `kb-version-bump.sh 1.5.0 1.6.0`. Its "remaining OLD mentions" list came back empty, and `cargo check --workspace --all-targets` finished clean.
- **Impact:** `node --test tests/plugin/*.test.cjs` passes 17 of 17, including "every manifest carries the same version" and "the manifest version reaches the crate, the workflow, the site, and the release notes".

**Note for the lead:** `node --test tests/plugin/` (a directory argument) fails on Node 24 with MODULE_NOT_FOUND. The working invocation, used by CI, `scripts/run_tests.sh`, and `scripts/release-preflight.sh`, is `node --test tests/plugin/*.test.cjs`.
