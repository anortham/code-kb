---
id: checkpoint_c8d87c0f
timestamp: 2026-09-22T00:50:54.741Z
tags:
  - release-v1.5.0
  - version-bump
git:
  branch: main
  commit: 18fcc90
  worktree: /home/murphy/source/code-kb
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: code-kb v1.5.0 release commit
type: checkpoint
---

## code-kb v1.5.0 release commit

**WHAT**: version 1.5.0 in the workspace manifest, the CLI's core dependency, the four plugin manifests, the release workflow default, and the site badge and stylesheet query; lockfile synced with `cargo check`. `docs/release-notes/v1.5.0.md` now states that the pin is in place.

**Release path**: Windows guest run on this commit, `scripts/release-preflight.sh`, fast-forward merge to main, push, green CI, annotated tag `v1.5.0`, Release Binaries workflow, record the six archive checksums in the release notes, publish `code-kb-core` then `code-kb-cli` to crates.io, verify `cargo install` and `cargo binstall`.

**IMPACT**: users get the Qt and QML support described in the README "Qt and QML" section with julie-extract 3.2.0 bundled in every archive.
