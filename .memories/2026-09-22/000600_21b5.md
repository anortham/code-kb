---
id: checkpoint_21b5861f
timestamp: 2026-09-22T00:06:00.362Z
tags:
  - qt
  - qml
  - plan-025
  - v1.5.0
  - docs
git:
  branch: main
  commit: 18fcc90
  worktree: /home/murphy/source/code-kb
  files:
    - docs/plans/025-qt-qml-first-class-support.md
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "feat/v1.5-qt: docs re-measured after the julie fix round, branch
  complete except the pin bump"
type: checkpoint
next: Commit the doc refresh, re-run the two doc tests, then report to the owner
  with the release steps that need approval.
---

## feat/v1.5-qt: docs re-measured after the julie fix round, branch complete except the pin bump

**WHAT**
- B1 to B6, B2b (one row per site; Qt module aliases never resolve locally), B3b (member accesses grouped per file), and B8 (plan on the branch, README "Qt and QML", site entry, invariant 6 rules, release notes v1.5.0) are committed. Branch gate at 553a68b: fmt, clippy, 21 test binaries green.
- After julie wave 5 the corpus indexes were rebuilt with `scan --force` and the documented numbers re-measured: skeleton Button.qml 71 lines; `refs BarWidget` 15 rows (12 `extends`, 3 handler rows); `refs Color` 53 files; `refs clicked` 4 calls, 1 `handler`, 15 candidates; Kirigami `refs Page` 60 rows with 7 `extends` (four inline components). README, release notes, and the plan status now say so; the plan's stale "nothing implemented yet" sentence is gone.
- The session's four earlier checkpoint files moved from the main checkout into this worktree's `.memories/` so they travel with the branch.

**Open**: B7 pin bump to julie-extract 3.2.0 needs the published release assets and their six checksums (owner releases julie first). The main checkout still holds an untracked copy of plan 025 that will collide with the branch copy at merge time; delete it before merging. `lookup waitForW` falls back to single-letter full-text matches (pre-existing search behavior, not fixed here).
