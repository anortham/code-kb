---
id: checkpoint_c534c42b
timestamp: 2026-09-22T00:50:49.898Z
tags:
  - release-v1.5.0
  - pin-bump
  - julie-3.2.0
git:
  branch: main
  commit: 18fcc90
  worktree: /home/murphy/source/code-kb
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: code-kb pins julie-extract 3.2.0
type: checkpoint
---

## code-kb pins julie-extract 3.2.0

**WHAT**: `scripts/julie-pins.json` carries version 3.2.0 and the six archive SHA-256 values, each hashed locally from the downloaded release archive and matched against the GitHub API digest. `PINNED_JULIE_VERSION` in `crates/code-kb-core/src/sync.rs` follows, as does invariant 7 text in CLAUDE.md and AGENTS.md. `.tools/julie-extract` was restored from the pin (checksum verified, reports 3.2.0).

**WHY the test fixture changed**: `resolution_test.rs` inserted synthetic `extends` pending rows because the 3.1.3 extractor emitted none; 3.2.0 emits the real rows, so the fixture doubled them. The fixture now uses the scanned repository as is, and the test exercises real extractor output.

**IMPACT**: with the pinned extractor every code-kb test runs against the QML object model; the workspace suite (21 binaries), clippy with `-D warnings`, fmt, and the plugin manifest tests pass.
