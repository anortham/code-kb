---
id: checkpoint_9a63ba00
timestamp: 2026-09-16T00:31:29.618Z
tags:
  - release
  - v1.0.3
  - docs
git:
  branch: main
  commit: affd25e
  files:
    - .claude-plugin/marketplace.json
    - .claude-plugin/plugin.json
    - .codex-plugin/plugin.json
    - .github/workflows/release-binaries.yml
    - Cargo.lock
    - Cargo.toml
    - crates/code-kb-cli/Cargo.toml
    - docs/site/index.html
    - plugin.json
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: v1.0.3 release prep
briefId: code-kb-lightweight-code-intelligence-engine-mcp-s
type: checkpoint
---

## v1.0.3 release prep

### WHAT
- Docs pass before release (affd25e): syntax-validation scope corrected in the tool description, README, and site; `telemetry_summary`, `stats`, and `bug-report` added to the catalogs; site leads with plugin installs and shows Codex TOML; release guide gains the crates.io section and the site badge in the bump list; v1.0.3 notes drafted.
- Version bump to 1.0.3 in Cargo.toml, the CLI dependency, four plugin manifests, the release workflow default, and the site badge. Lockfile synced.
- Both crate manifests now package the README; the CLI crate has cargo-binstall metadata.

### WHY
- The 1.0.2 crates on crates.io had no README because the manifests lacked a `readme` field. A published version cannot be changed, so v1.0.3 carries the fix.

### VERIFIED
- release-preflight PASSED for v1.0.3 (sync, versions, fmt, clippy, full suite, packaging, win VM).

### NEXT
- Commit, push, CI, tag v1.0.3, release build, checksums, publish both crates, verify installs.

