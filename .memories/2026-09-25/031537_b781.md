---
id: checkpoint_b781e1a8
timestamp: 2026-09-25T03:15:37.112Z
tags:
  - release
  - 2.2.1
  - dogfood
git:
  branch: main
  commit: "8950621"
  files:
    - .claude-plugin/skills/code-kb/SKILL.md
    - AGENTS.md
    - CLAUDE.md
    - README.md
    - crates/code-kb-cli/src/main.rs
    - crates/code-kb-cli/src/mcp/server.rs
    - crates/code-kb-core/src/formatters.rs
    - crates/code-kb-core/src/lib.rs
    - crates/code-kb-core/src/models.rs
    - crates/code-kb-core/src/ops.rs
    - crates/code-kb-core/src/queries.rs
    - crates/code-kb-core/src/slicer.rs
    - crates/code-kb-core/src/telemetry.rs
    - crates/code-kb-core/tests/references_test.rs
    - docs/release-notes/v2.2.1.md
    - docs/site/index.html
    - skills/code-kb/SKILL.md
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "code-kb 2.2.1: all dogfood round-1 defects and gaps fixed"
next: cargo build --release; corpus check vs v2.2.0; review diffs;
  scripts/release-dogfood.sh round 2; sort claims; report to user before any tag
---

## code-kb 2.2.1: all dogfood round-1 defects and gaps fixed

- **WHAT:** I fixed defects 1–13 and gaps a–g from dogfood round 1:
  - body source with decorators;
  - same-site row merge;
  - related tests through references;
  - skeleton module variables and the `in run()` note;
  - route template as written;
  - Markdown links are no longer imports, and `display_kind`;
  - lookup prefix ranking, the nested penalty, and `Class::test` names;
  - the import summary on callers;
  - the no-facts category list;
  - telemetry full errors and UTC;
  - outline test and fixture counts;
  - `setUp` is `setup`;
  - fixture-built receivers;
  - `__call__` builders need a shared word;
  - the stem must match whole words.
- **WHY:** The user chose "Fix everything" before julie 3.6.2 and code-kb 2.2.1 ship.
- **HOW:** Most changes are in `queries.rs`, `formatters.rs`, `ops.rs`, and `slicer.rs`. The julie 3.6.2 local build is in `.tools/julie-extract` (commit ea793be3, not pushed). CLAUDE.md and AGENTS.md now document the rules. The release notes are updated.
- **Verification:** fmt, clippy, 497 Rust tests, and 17 plugin tests pass.
- **IMPACT:** Next are the corpus check and dogfood round 2. Two limits still need a user decision:
  - `test_cli_invoke` needs return-type inference for a local variable;
  - julie drops the module-level `main()` call in `__main__.py`.
