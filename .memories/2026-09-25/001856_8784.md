---
id: checkpoint_87847b59
timestamp: 2026-09-25T00:18:56.223Z
tags:
  - release
  - 2.2.1
  - blast-radius
  - lookup
git:
  branch: main
  commit: 2ef308e
  files:
    - .claude-plugin/marketplace.json
    - .claude-plugin/plugin.json
    - .codex-plugin/plugin.json
    - .github/workflows/release-binaries.yml
    - AGENTS.md
    - CLAUDE.md
    - Cargo.lock
    - Cargo.toml
    - README.md
    - crates/code-kb-cli/Cargo.toml
    - crates/code-kb-core/src/formatters.rs
    - crates/code-kb-core/src/queries.rs
    - crates/code-kb-core/src/sync.rs
    - crates/code-kb-core/tests/blast_radius_test.rs
    - docs/release-notes/v2.2.1.md
    - docs/site/index.html
    - plugin.json
    - scripts/julie-pins.json
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "code-kb 2.2.1 candidate: julie 3.6.1 pin plus two more fixes from the
  corpus check"
type: checkpoint
next: Commit, push, CI, win-test, final corpus check, dogfood, preflight; stop
  before tag
---

## code-kb 2.2.1 candidate: julie 3.6.1 pin plus two more fixes from the corpus check

**WHAT:** Pinned julie-extract 3.6.1 (released 2026-09-24, tag at 52b605fd; Python `self.x` signatures keep `self.`). Bumped every manifest to 2.2.1. Two new fixes after the first corpus run: (1) `format_find_symbol_results` folds only imports whose name equals the query or ends with `.query`/`::query` (it had folded `readline`, `user`, `users`, `ThreadingHTTPServer` under the query); (2) `compute_blast_radius` stem/module matches count only when the file holds an `is_test` symbol, its name split on `_ - .` has test/tests/spec/specs/tst as a word, or it GLOBs `*Test.*`/`*Tests.*`/`*Spec.*`/`*Specs.*` (removed fixtures, templates, `search-inspect.py`, seed files).

**WHY:** User rule: report and fix every defect a release check finds before the tag.

**HOW:** Failing tests first (`a_matched_file_counts_as_a_test_only_when_its_name_or_symbols_say_so`, `exact_lookup_folds_only_imports_named_exactly_like_the_query`). First corpus run hit `Disk quota exceeded` in /tmp (my old scratch data, 25 GB tmpfs quota) plus false slow scans from memory pressure; cleaned scratch and reran.

**IMPACT:** 467 tests pass, clippy clean, corpus PASS vs v2.2.0 with all diffs reviewed. Next: commit, push, CI, Windows guest, final corpus after HEAD, dogfood, preflight, report before tag. julie evidence commit still has CORPUS_PLACEHOLDER to fill.
