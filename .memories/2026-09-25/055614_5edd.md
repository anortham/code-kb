---
id: checkpoint_5eddab47
timestamp: 2026-09-25T05:56:14.469Z
tags:
  - dogfood
  - release-2.2.1
  - outline
  - telemetry
git:
  branch: main
  commit: b609b86
  files:
    - README.md
    - crates/code-kb-core/src/formatters.rs
    - crates/code-kb-core/src/ops.rs
    - crates/code-kb-core/src/queries.rs
    - crates/code-kb-core/src/telemetry.rs
    - docs/release-notes/v2.2.1.md
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Dogfood round 4 fixes (code-kb side)
next: Rerun the corpus check against v2.2.0 and review the delta against the r3
  report. Then run dogfood round 5 and report to the user before any tag.
---

## Dogfood round 4 fixes (code-kb side)

- **WHAT:**
  - `telemetry_summary` label is now "Tokens Saved, upper bound", and the text names the baseline. Changed in `telemetry.rs` and `README.md`.
  - Outline folder rows now count unsupported files: `docs/ (1 indexed file, 86 unsupported)`.
    - `queries::for_each_unsupported_path` streams the paths and replaces `count_unsupported_files`.
    - `formatters::add_unsupported_to_outline` adds each file to the deepest folder the outline shows.
    - `outline_components` is shared with `add_path_to_outline`.
- **WHY:** These fix round-4 dogfood claims. Before, `docs/` showed only `conf.py`, with no sign of its 86 `.rst` files.
- **Verified:**
  - fmt and clippy pass with `-D warnings`.
  - 514 Rust tests pass, 0 fail.
  - 17 plugin tests pass.
  - The hermes-agent outline takes 0.42–0.45 s warm, the same as before.
- **julie:** the round-4 fixes are in commit `77cd5314`, local only. They cover lambda lines, `#:` docs on `self.x`, and `self.x` rows beside a property.
- **IMPACT:** Next: the corpus check and dogfood round 5. Then report to the user before any tag.
