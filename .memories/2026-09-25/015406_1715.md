---
id: checkpoint_17156e1e
timestamp: 2026-09-25T01:54:06.259Z
tags:
  - release-gate
  - v2.2.1
  - julie-3.6.2
  - dogfood
git:
  branch: main
  commit: 36d8595
  files:
    - AGENTS.md
    - CLAUDE.md
    - README.md
    - crates/code-kb-core/src/formatters.rs
    - crates/code-kb-core/src/queries.rs
    - crates/code-kb-core/src/sync.rs
    - crates/code-kb-core/tests/references_test.rs
    - docs/RELEASING.md
    - docs/release-notes/v2.2.1.md
    - docs/site/index.html
    - scripts/julie-pins.json
    - scripts/release-dogfood-prompt.md
    - scripts/release-dogfood.sh
    - scripts/release-preflight.sh
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Dogfood release gate added; corpus review found 6 more 2.2.1 defects, all fixed
type: decision
decision: Release only when a full dogfood round finds zero open defects,
  enforced by preflight step 10.
next: Commit locally; rerun corpus check; run scripts/release-dogfood.sh until a
  round has no defect; report to the user; then julie 3.6.2 tag.
---

## Dogfood release gate added; corpus review found 6 more 2.2.1 defects, all fixed

**WHAT**
- New `scripts/release-dogfood.sh` + `scripts/release-dogfood-prompt.md`: one Claude + Codex round on flask worktrees, writes `target/release-dogfood/report.md`.
- `scripts/release-preflight.sh` step 10/10: report needs `## Result: PASS`, no `- [open]` line, julie pin version, round commit ancestor of HEAD, no `crates/` change since (Cargo.toml excluded).
- `docs/RELEASING.md` step 7 (dogfood gate, stop rule, only the user defers), step 8 (tag julie only after code-kb corpus + dogfood pass on the local julie), bump committed last. julie `docs/release.md` got a "Downstream Gate" section.
- Corpus review (julie 3.6.2 candidate vs v2.2.0) defects fixed:
  1. blast_radius listed a pytest fixture that takes a fixture as a test -> `fixture_users` now walks fixture chains.
  2. Tests reached through a fixture said "builds `Flask`" -> now "uses fixture `app`, which builds `Flask`...".
  3. Skeleton showed every `variable` child (YAML/JSON keys, HTML img, Java static-block locals) -> only Python class attributes (parent kind class).
  4. Value-row rule: CSS `[a^=b]`, C# attribute `(X = ...)`, `>=` counted as assignment; `#define` kept newline; `;;` on Java fields; `{ … {` on containers and hidden-body rows -> `assigns_a_value` (depth-0 `=`, not an operator), trim `;`, container rows and hidden-body rows use `container_signature`.
  5. Shadow rule hid same-file targets (nlohmann `get`, zod `extends Base` via `export` row) -> applies only when target is in another file; import/export rows are not shadows.
  6. xUnit test-class constructor labelled `fixture` -> non-pytest lifecycle rows labelled `setup`, tests of the class added (`tests_in_class`).

**WHY** The user asked why defects keep appearing after releases. Root cause: corpus check only finds changed output; dogfood had no gate or stop rule; julie was tagged before the code-kb check; bump pushed early.

**IMPACT** 483 Rust tests + 17 plugin tests pass, clippy clean. Next: commit locally, rerun corpus, run dogfood rounds until clean, then report to the user before any julie tag.
