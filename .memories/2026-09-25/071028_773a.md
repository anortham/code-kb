---
id: checkpoint_773a185b
timestamp: 2026-09-25T07:10:28.858Z
tags:
  - release-2.2.1
  - paused
  - corpus
git:
  branch: main
  commit: 47fb285
  files:
    - scripts/release-corpus-check.py
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Stopped at the user's request: no release yet"
next: Wait for the user. Before any release, rerun the corpus check and dogfood
  round 5.
---

## Stopped at the user's request: no release yet

- **WHAT:** `scripts/release-corpus-check.py` now skips generated `lambda_N` names in the probe sample. It filters after sampling, so the other probes stay the same.
- **WHY:** julie 3.6.2 counts lambda lines from 1, so `lambda_605` became `lambda_606`. Five probes then "broke", and the corpus run for `47fb285` reported FAIL.
- **Review:** I read the delta of that run against the r4 report. Every change is correct:
  - zod finds new real callers (`_maxLength`, `ZodSymbol`).
  - The `ru.ts` targets now point to `v4/core/util.ts`.
  - The rest are the outline counts and the fixed displays.
- **User decision:** "I don't want to do another release yet. Stop after current tasks finish."
- **Owed before any release:**
  1. Rerun the corpus check. It must PASS with the script fix.
  2. Run dogfood round 5.
  3. Report to the user.
- **Open user decisions:**
  - the ceiling of the `possible` rows heuristic;
  - `test_cli_invoke`;
  - endpoint names;
  - module-level calls;
  - C# verbatim strings;
  - the jq `#ifdef` brace limit;
  - the proposed 2.2.1 feature freeze.
