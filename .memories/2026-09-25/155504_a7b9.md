---
id: checkpoint_a7b9e84d
timestamp: 2026-09-25T15:55:04.816Z
git:
  branch: fix/dogfood-gaps
  commit: "400e959"
  files:
    - AGENTS.md
    - CLAUDE.md
    - crates/code-kb-core/src/queries.rs
    - crates/code-kb-core/tests/references_test.rs
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Closed two deferred v2.2.1 dogfood gaps in code-kb (branch fix/dogfood-gaps)
type: learning
---

## Closed two deferred v2.2.1 dogfood gaps in code-kb (branch fix/dogfood-gaps)

- **WHAT:** `pending_target_predicate` (queries.rs) now matches a method call on a local variable built by a call (`runner = app.test_cli_runner()`), through the class named or the callee's `returnType` metadata; fixtures follow the same rule and are scoped to the test file or an ancestor `conftest.py`. Callees now include `self.attr()` calls to class attributes (`callee_target_kind`, and the self rule allows depth 0 for variable/field targets).
- **WHY:** dogfood gaps: blast_radius(cli.py) missed test_cli_invoke and test_init_db_command; wsgi_app callees missed should_ignore_error. The julie index already had `returnType` and local variable rows, so no julie change was needed.
- **Perf lessons (hermes-agent, 988k symbols):**
  - `fixture.name = x AND fixture.test_lifecycle = 1` made SQLite scan the whole test_lifecycle index per row; add `fixture.kind IN ('function','method')` and `+test_lifecycle`.
  - Joining a CTE name column to `symbols` made SQLite build an AUTOMATIC index over all classes per evaluation (blast_radius 1.8 s -> 8.8 s); `CROSS JOIN` forces the name index.
  - Profile by dumping the SQL (temporary eprintln of the statement) and timing variants in `sqlite3 -readonly`.
- **IMPACT:** all hermes probes are equal to or faster than v2.2.1 (refs get 30 s -> 18 s, context execute 3 s -> 0.8 s). Corpus check PASS on all 15 projects, 0 changed output. /tmp tmpfs is full of other sessions' scratch; run corpus with TMPDIR on disk.
- **Still open:** blast_radius possible-test heuristic noise/misses (handle_user_exception); Flask add_url_rule endpoint names and decorator-vs-call route line (julie-side).
