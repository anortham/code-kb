---
id: checkpoint_61a94ec2
timestamp: 2026-09-15T17:32:18.534Z
tags:
  - plan-017
  - version-guard
  - facts-level
  - parent-pid
  - v1.0.2
  - julie-2.43.0
git:
  branch: fix/extractor-seam
  commit: 843eac5
  files:
    - .claude-plugin/plugin.json
    - .codex-plugin/plugin.json
    - .github/workflows/release-binaries.yml
    - AGENTS.md
    - CLAUDE.md
    - Cargo.lock
    - Cargo.toml
    - README.md
    - crates/code-kb-cli/Cargo.toml
    - crates/code-kb-cli/src/main.rs
    - crates/code-kb-cli/src/mcp/server.rs
    - crates/code-kb-cli/tests/adversarial_m2_server_test.rs
    - crates/code-kb-cli/tests/cli_test.rs
    - crates/code-kb-core/src/lib.rs
    - crates/code-kb-core/src/sync.rs
    - crates/code-kb-core/tests/freshness_test.rs
    - docs/plans/017-product-cohesion-assessment.md
    - docs/release-notes/v1.0.2.md
    - scripts/julie-pins.json
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Plan 017 fixes applied on branch fix/extractor-seam (code-kb 1.0.2)
briefId: code-kb-lightweight-code-intelligence-engine-mcp-s
next: Finish julie gates, commit both branches, run win-test on both repos, then
  ask the user to approve the julie 2.43.0 release and fill sha256 pins.
---

## Plan 017 fixes applied on branch fix/extractor-seam (code-kb 1.0.2)

**WHAT:**
- `sync.rs`: pinned-first extractor discovery (`installed_extractor_version`), guard renamed to `ensure_index_matches_extractor(ws, db, version)` comparing recorded `binary_version` and `index_level` against the binary in use and the `facts` level; scans pass `--level facts` on new artifacts and `--parent-pid` on Unix; `schema_incompatible` on an existing artifact removes it and rescans.
- Pin bumped to julie-extract 2.43.0 (`scripts/julie-pins.json` sha256 fields blank until the julie release is published). code-kb version 1.0.2, release notes `docs/release-notes/v1.0.2.md`, README cargo note, CLAUDE.md/AGENTS.md invariant 7 updated.
- Tests: guard keeps a non-pinned index (`freshness_test`), level guard rebuilds, empty artifact recovery, CLI scan prefers pinned over vendored `.tools`, passes own pid, requests facts level. Worktree WAL test fixture now writes its 200 files to disk so reconcile does not bulk-scan a hand-made DB.

**WHY:** Plan 017 findings 1, 2, 3, 7. The rebuild loop was reproduced deterministically on a two-file repo.

**HOW:** TDD, red then green, mutation check on the level guard. Full workspace suite green with `.tools/julie-extract` = local 2.43.0 build; fmt and clippy clean.

**IMPACT:** code-kb now requires julie-extract 2.43.0 (facts level). Blocked on publishing julie 2.43.0 before pins sha256 and CI can pass. Julie side lives on `feat/facts-level` in ~/source/julie-extractors.
