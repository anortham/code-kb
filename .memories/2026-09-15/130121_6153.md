---
id: checkpoint_615394b7
timestamp: 2026-09-15T13:01:21.139Z
tags:
  - review
  - plan-016
  - search-ranking
  - version-guard
  - telemetry
git:
  branch: main
  commit: adb865c
  files:
    - .claude-plugin/skills/code-kb/SKILL.md
    - README.md
    - crates/code-kb-cli/src/main.rs
    - crates/code-kb-cli/src/mcp/server.rs
    - crates/code-kb-cli/src/routing-block.md
    - crates/code-kb-cli/tests/adversarial_m3_server_test.rs
    - crates/code-kb-cli/tests/cli_test.rs
    - crates/code-kb-cli/tests/mcp_test.rs
    - crates/code-kb-core/src/lib.rs
    - crates/code-kb-core/src/ops.rs
    - crates/code-kb-core/src/queries.rs
    - crates/code-kb-core/src/sync.rs
    - crates/code-kb-core/src/telemetry.rs
    - crates/code-kb-core/tests/freshness_test.rs
    - docs/plans/010-master-implementation-plan.md
    - docs/plans/016-post-v1.0.1-assessment.md
    - docs/plans/2026-09-14-pin-julie-extract-v2.42.3-and-v1.0-release.md
    - hooks/code-kb-routing-block.md
    - skills/code-kb/SKILL.md
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Post-v1.0.1 assessment vs julie/miller applied (plan 016)
briefId: code-kb-lightweight-code-intelligence-engine-mcp-s
type: checkpoint
next: Commit the 18 changed files plus docs/plans/016 if the user approves;
  consider `cargo install --path crates/code-kb-cli --force` to refresh the PATH
  binary.
---

## Post-v1.0.1 assessment vs julie/miller applied (plan 016)

**WHAT:** Reviewed code-kb against julie and miller, wrote `docs/plans/016-post-v1.0.1-assessment.md`, and applied the approved fixes on `main` (uncommitted).

- Routing block + SKILL.md: `rg` fallback for literal text; name-matching caveat on `find_references`.
- `queries.rs` FTS ranking: definitions before imports, code languages before markup/config, then BM25.
- `server.rs`: `lookup_symbol` accepts `symbol_name`/`symbol`; `code_kb_stats` alias removed.
- `ops.rs`: `file_skeleton` on a directory returns its outline.
- `sync.rs`: new `ensure_index_matches_pin` rebuilds the index once when `artifact_metadata.binary_version` differs from the pin (old file removed first because julie-extract refuses writes into older-schema artifacts). Called in `McpServer::new`, `bind_workspace`, and the CLI read path. Test in `freshness_test.rs`.
- `telemetry.rs`: deleted `migrate_legacy_workspace_telemetry` + 3 tests + dead helper; tokens-saved relabeled as an estimate for read tools only.
- Tests: three no-root server spawns now `current_dir` the temp repo so they stop reconciling the real DB and logging into the real repo.

**WHY:** Verdict was that the cut is right (11k src lines vs 134k julie, 395k miller; same tool count, simpler runtime). The findings came from telemetry errors, server logs, SQLite row counts, and live tool calls.

**HOW:** Direct read plus live checks, no agent fan-out. Verified: 210 tests pass across 17 suites, clippy -D warnings clean, fmt clean, AGENTS/CLAUDE and SKILL/routing copies in sync, live CLI/MCP checks for ranking, directory skeleton, alias, hook text.

**IMPACT:** Unmeasured question left open: cold-start cost of the 1:1 session model on repos >1000 files. Measure on ~/source/miller before any daemon discussion.
