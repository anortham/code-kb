---
id: checkpoint_03eb8c03
timestamp: 2026-09-15T00:01:37.309Z
tags:
  - release
  - v0.9.0
  - pre-flight
git:
  branch: main
  commit: 472dffc
  files:
    - .claude-plugin/marketplace.json
    - .claude-plugin/plugin.json
    - .codex-plugin/plugin.json
    - .github/workflows/release-binaries.yml
    - Cargo.lock
    - Cargo.toml
    - crates/code-kb-cli/Cargo.toml
    - docs/site/index.html
actor:
  harness: antigravity-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Version Bump to v0.9.0 and Release Pre-Flight Verification
briefId: code-kb-lightweight-code-intelligence-engine-mcp-s
type: checkpoint
evidence:
  - ./scripts/release-preflight.sh passed all 7 checks
  - cargo test --workspace (194 tests passed)
  - cargo clippy --workspace --all-targets -- -D warnings clean
symbols:
  - code-kb
next: Commit release bump, tag v0.9.0, push main and tag to origin, and monitor
  GitHub Actions release-binaries workflow
---

## Version Bump to v0.9.0 and Release Pre-Flight Verification

- **WHAT:** Bumped version from `0.8.0` to `0.9.0` across workspace manifests (`Cargo.toml`, `crates/code-kb-cli/Cargo.toml`), plugins (`.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `.codex-plugin/plugin.json`), workflow (`.github/workflows/release-binaries.yml`), documentation site (`docs/site/index.html`), and locked in `Cargo.lock`.
- **WHY:** Incorporates major core/CLI/MCP enhancements: target identity preservation in pending call relationships, blast radius seed disambiguation with path hints, optional `file_path` on `find_references`, token-dense result caps disclosure, and path filter + category aliases on `find_structural_facts`.
- **HOW:** Ran `./scripts/release-preflight.sh` to verify all 7 pre-flight invariants (sync contracts, manifest consistency, rustfmt, clippy, workspace test suite, cargo package dry-run, Windows guest check).
- **IMPACT:** Ready to tag and publish `v0.9.0` release.
