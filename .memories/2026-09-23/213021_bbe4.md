---
id: checkpoint_bbe41d3a
timestamp: 2026-09-23T21:30:21.406Z
tags:
  - issue-3
  - project-root
  - 2.1.0
  - review-round-a
git:
  branch: feat/project-root
  commit: e14e37b
  worktree: /home/murphy/source/code-kb/.claude/worktrees/project-root
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Branch review round A (Task 1 worker): R4, R3 helper, R7 docs"
type: checkpoint
symbols:
  - Workspace::from_project_root_with_homes
  - Workspace::nested_project_root
next: "Round B: the server rejects an absolute path argument inside a nested
  project with Workspace::nested_project_root."
---

## Branch review round A (Task 1 worker): R4, R3 helper, R7 docs

- **R4 (dotfiles home):** `from_project_root_with_homes` in `crates/code-kb-core/src/workspace.rs` now searches again when `find_workspace_root` returns a home candidate or a filesystem root. It walks from the canonical input (its folder for a file) up to, but not including, that folder and accepts the nearest `is_project_root` folder. With no such folder, the home or root refusal stays. `~/.git` plus `~/work/app/Cargo.toml` now binds `~/work/app`.
- **R3 helper:** new `pub fn Workspace::nested_project_root(&self, path: &Path) -> Option<PathBuf>`. It reuses `resolve_path` (same file://, relative, Windows, and symlink rules), then walks up while strictly inside `canonical_root` and returns the nearest folder with `.git` or `.code-kb/artifact.db`, canonical and normalized. Language markers never count. Server wiring is round B.
- **R7 docs:** `AGENTS.md` and `CLAUDE.md` invariant 1 now state the true walk rule and that an absolute path inside a nested worktree or submodule counts as outside `project_root`. README "How code-kb Finds Your Project" got the same nested-path sentence.
- **Verification:** 6 new unit tests (red first). `cargo test -p code-kb-core` 346 passed; clippy `-D warnings` clean; `fmt --check` clean; `cmp AGENTS.md CLAUDE.md` identical.
