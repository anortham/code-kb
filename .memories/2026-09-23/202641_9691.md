---
id: checkpoint_969101bf
timestamp: 2026-09-23T20:26:41.844Z
tags:
  - issue-3
  - project-root
  - docs
  - 2.1.0
git:
  branch: main
  commit: 58b9468
  worktree: /home/murphy/source/code-kb
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Task 4 (2.1.0, issue #3): docs and agent text for the required `project_root`"
type: checkpoint
---

## Task 4 (2.1.0, issue #3): docs and agent text for the required `project_root`

**What:** The docs and the agent-facing text now tell agents to pass `project_root` on every tool call except `telemetry_summary`.

- **AGENTS.md / CLAUDE.md:** invariant 1 is now "Required `project_root` on Every Tool". The old 7-item binding list is gone. The still-true parts moved: startup pre-warm, automatic index creation, silent `workspace`/`root` aliases, path rules, `--db` for the launch root only, telemetry, CLI `--root`. Invariant 5 has the aliases, and scoped filters are relative to `project_root`. Invariant 6 has the 5 s wait, the "Indexing <root> started" answer, and one watcher that restarts on a root switch.
- **Routing block (both copies):** one `project_root` rule replaces the `EnterWorktree` workaround. One example call with `project_root`. Size is 2588 bytes (limit 3072).
- **Skill (both copies):** new "Required `project_root`" section, parameter notes, and core invariant.
- **README and docs/site/index.html:** the "How code-kb Finds Your Project" section is rewritten. No GUI config uses `--root` now. A sentence above each tools table says that every tool except `telemetry_summary` requires `project_root`.
- **ADR 001:** a status line says the ADR is superseded in 2.1.0 and links to the plan.
- **cli_test.rs:** the SessionStart hook test asserts `project_root`, and asserts that `EnterWorktree` and `Binding is server-wide` are gone. `mcp_search_names` now sends `project_root`.

**Why:** Issue #3. A root held in server state goes stale after a move to a worktree. The user chose a required per-call `project_root`.

**Verification:** `cargo test -p code-kb-cli --test cli_test` 47/47 passed. The sync `cmp` and `diff` checks pass. `node --test tests/plugin/*.test.cjs` 17/17 passed. `cargo fmt --check` is clean.

**Note:** On Node 24, `node --test tests/plugin/` fails because Node treats the directory as a module. Use the `*.test.cjs` glob.
