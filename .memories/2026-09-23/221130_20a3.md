---
id: checkpoint_20a38267
timestamp: 2026-09-23T22:11:30.502Z
tags:
  - issue-3
  - project-root
  - mcp
  - worktree
  - v2.1.0
  - sdd
git:
  branch: feat/project-root
  commit: 1f7deb7
  worktree: /home/murphy/source/code-kb/.claude/worktrees/project-root
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Issue #3 fix implemented on feat/project-root (2.1.0 plan)"
type: checkpoint
next: "Final gates at the docs commit, then finishing-a-development-branch:
  offer merge to main locally or keep the branch; push and 2.1.0 release need
  approval."
---

## Issue #3 fix implemented on feat/project-root (2.1.0 plan)

**WHAT**
- Every MCP tool except `telemetry_summary` now requires `project_root` (plan `docs/plans/2026-09-23-mcp-roots-worktree-binding.md`).
- Core: `Workspace::from_project_root` resolves and refuses roots (relative, missing, filesystem root, home, no marker and no index); `nested_project_root` (git only); dotfiles-home fallback shared with `Workspace::discover`.
- Server: resolution once per call in `handle_call_tool`; one active index; `switch_root` with parked prepares; every prepare owns its watcher; 5 s bounded wait (`CODE_KB_INDEX_WAIT_MS`) with an "Indexing <root> started" answer; outside-path and nested-project refusals; `--db` pins the launch root only (and counts as a markerless launch root's index); refused calls record the launch root; initialize roots and path rebinding deleted; startup pre-warm skipped for refused roots.
- CLI: refuses a home or filesystem root like the tools; auto-index only for accepted roots.
- Docs: AGENTS/CLAUDE invariants 1, 5, 6; routing block; skills; README; site; ADR 001 superseded.

**WHY**
- Issue #3: a running server answered from the launch root after EnterWorktree, and path arguments rebound the whole server. MCP roots are unreliable (Codex/Grok none, agy `[]`).

**HOW**
- Subagent-driven: 4 tasks (core, server, adversarial tests, docs in an isolated worktree), then 4 review rounds (workflow review 10 findings, fix-diff review 5, round C review 1, all fixed). Ledger: `.razorback/sdd/.../progress.md` (git-ignored).

**IMPACT / verification**
- cf147ec: Linux gate 452 tests + 17 node tests pass; Windows 443 pass. Black-box (Herdr, flask-base, branch release binary): Claude x4, Codex x2, Grok -w x1 all pass: every call after the worktree step named the worktree.
- Not done: push, version bump to 2.1.0, release notes, merge. Those need the user's approval.
