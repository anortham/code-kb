---
id: checkpoint_bb8315fd
timestamp: 2026-09-23T22:38:01.242Z
tags:
  - v2.1.0
  - codex-review
  - pre-release
git:
  branch: fix/pre-2.1.0
  commit: 47e2fb2
  files:
    - AGENTS.md
    - CLAUDE.md
    - README.md
    - crates/code-kb-cli/src/mcp/server.rs
    - crates/code-kb-cli/tests/mcp_test.rs
    - crates/code-kb-core/src/workspace.rs
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Codex review of unreleased 2.1.0: 4 findings fixed"
type: checkpoint
next: Run the Windows suite on the new commit, then ask the user about merging
  into main and the 2.1.0 release.
---

## Codex review of unreleased 2.1.0: 4 findings fixed

- **WHAT:** A Codex review of `origin/main..47e2fb2` returned 4 findings. All are fixed on `fix/pre-2.1.0`:
  1. A relative path into a nested git worktree bypassed the nested-project refusal. The check now runs on every path argument.
  2. Offline reconcile errors were discarded silently. They are now logged as warnings. The calls still answer, because failing every call would make the tool unusable. The released 2.0 code has the same silent behavior.
  3. A finished parked prepare kept its root's watcher. `drop_finished_parked_prepares` now runs at every call and every switch.
  4. On Windows, a drive-relative `project_root` (`C:src`, `C:`) passed as absolute. `from_project_root` now uses `Path::is_absolute`.
- **WHY:** The user wants these fixed before the 2.1.0 release.
- **IMPACT:** 457 Linux tests and 17 node tests pass. AGENTS.md, CLAUDE.md, and README are updated for the nested-path rule and the watcher rule. Next step: the Windows run.
