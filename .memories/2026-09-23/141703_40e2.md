---
id: checkpoint_40e2b7a0
timestamp: 2026-09-23T14:17:03.535Z
tags:
  - release
  - v2.0.2
  - windows
  - verification
  - julie-3.5.0
git:
  branch: main
  commit: 0fb2625
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Verified the local 2.0.2 release candidate on Windows
type: checkpoint
evidence:
  - "Windows NTFS cargo test --workspace --locked: 393 passed, 0 failed"
  - Windows julie-extract 3.5.0 x86_64 checksum verified
  - "Linux cargo test --workspace --locked: 399 passed, 0 failed"
next: Keep the release candidate local until explicit push/release approval;
  after pushing, wait for exact-commit CI before tagging.
---

## Verified the local 2.0.2 release candidate on Windows

**WHAT:** Synced candidate commit `450e06a` to the Windows NTFS guest, restored and checksum-verified julie-extract 3.5.0 x86_64, and ran the full workspace Rust suite.

**WHY:** Windows is a first-class release target; Linux results alone do not cover path identity, file locking, or extractor packaging on NTFS.

**HOW:** `win-test sync code-kb 450e06a...`, `win-test run code-kb -- powershell -File scripts/restore-julie-extract.ps1`, then `win-test run code-kb -- cargo test --workspace --locked`. The suite passed 393 tests with zero failures. Logs and hashes are in `~/.code-kb/perf-audits/2026-09-23-julie-3.5.0/`.

**IMPACT:** The local Linux, Windows, plugin, formatting, Clippy, and package checks are complete. Push approval, exact-commit GitHub CI, tagging, and publication remain.
