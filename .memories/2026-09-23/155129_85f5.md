---
id: checkpoint_85f5c63b
timestamp: 2026-09-23T15:51:29.515Z
tags:
  - bug-fix
  - issues-2-3-4
  - release-hold
git:
  branch: main
  commit: 1373ba8
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Addressed new issue #2 comment and issues #3 and #4 in v2.0.2 candidate"
type: checkpoint
evidence:
  - "cargo test --workspace --locked: 405 passed"
  - "node --test tests/plugin/*.test.cjs: 17 passed"
  - "cargo clippy --workspace --all-targets --locked -- -D warnings: passed"
  - "cargo build --release --locked: passed"
  - Real Terraform and AccessIQ release-binary checks passed
symbols:
  - render_symbol_skeleton
  - format_symbol_body
  - format_context_slice
  - init_logging
  - test_mcp_worktree_rebind
next: Move checkpoint to task worktree, commit locally, run Windows issue tests
  on exact commit, then final Claude review and any corrections. Recheck live
  issues before stopping.
---

## Addressed new issue #2 comment and issues #3 and #4 in v2.0.2 candidate

- **WHAT:** Same-line enums now show indexed members in `file_skeleton`; `get_symbol_body` and `get_symbol_context` print an expression body once when Julie stores it in both signature and body span. MCP hook and setup docs explain absolute-path rebinding after `EnterWorktree`, with tests for unscoped lookups after switching roots. `bug-report` creates `.code-kb/.gitignore` before writing logs.
- **WHY:** A new public issue #2 comment added AccessIQ records/enums. Issue #4 reported duplicate C# body text, and issue #3 reported stale MCP results after entering a worktree plus an unignored logs directory. The reporter's extractor facts already contained symbols and body spans.
- **HOW:** Core formatting deduplicates only an exact body suffix and expands a single-line enum when it has child symbols. The existing absolute-path MCP rebind mechanism is documented and tested rather than adding a workspace parameter or speculative protocol behavior. Logging writes the ignore file with `create_new`, preserving an existing file. Upstream Julie tests intentionally assert expression bodies in C# signatures; no Julie change or version bump is needed.
- **IMPACT:** Updated Terraform 61ea339 four issue paths and AccessIQ f1fcd58 files pass with the release binary; a C# object-initializer body appears once. Rust workspace suite 405/405, plugin tests 17/17, formatting, Clippy, release build, and AGENTS/CLAUDE and routing-block equality pass. Windows exact-commit verification and final Claude review remain. User release hold remains in effect; no push, tag, or publish.
