---
id: checkpoint_ea51e035
timestamp: 2026-09-23T01:00:05.655Z
tags:
  - v2.0.1
  - claude-review
  - review-fix
  - blast-radius
  - test-prediction
  - release-prep
git:
  branch: review/v2.0-live-dogfood
  commit: de7c9a7
  files:
    - crates/code-kb-core/src/formatters.rs
    - crates/code-kb-core/src/queries.rs
    - crates/code-kb-core/tests/blast_radius_test.rs
    - docs/reviews/2026-09-22-v2.0-live-dogfood.md
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Claude review findings resolved before the patch release
type: checkpoint
impact: The patch candidate retains module test prediction without generic path
  false positives or misleading notices.
evidence:
  - Claude CLI 2.1.280 structured review completed and validated; medium and low
    findings independently reproduced
  - file_seed_keeps_specific_tests_ahead_of_module_matches failed before fix and
    passed after
  - "cargo test -p code-kb-core --test blast_radius_test: 16 passed"
  - "cargo clippy --workspace --all-targets --locked -- -D warnings: exit 0"
  - "cargo fmt --all -- --check and git diff --check: exit 0"
symbols:
  - compute_blast_radius_scoped_with_ids
  - format_blast_radius
  - file_seed_keeps_specific_tests_ahead_of_module_matches
next: Commit review fixes, then prepare and verify the v2.0.1 release candidate.
---

## Claude review findings resolved before the patch release

**WHAT:** Claude CLI reviewed the two v2.0 dogfooding commits and found one medium test-prediction issue plus one low output-wording issue. Updated `compute_blast_radius_scoped_with_ids` so file-stem tests rank before module matches, generic module names are skipped, module candidates are bounded, and only relevant test basenames are accepted. Updated blast-radius ceiling and empty-result text. Added a regression fixture covering generic module paths, tight limits, and unrelated nested tests; documented the review in the dogfood report.

**WHY:** The first module heuristic matched `cli` anywhere in a full test path and could return unrelated crate tests before a later file's exact stem test. Notices said 'stem-matched' even after module matching was added.

**HOW:** Reproduced both findings with failing tests, then used a separate bounded SQL candidate path for module terms and a basename check. Kept `cli` and `utils` when a test basename genuinely starts with the module name; blanket exclusion would lose useful predictions. Verified the fix against the live `server.rs` case. The candidate's settled 30-run p95 was 27.00 ms versus 49.61 ms in the earlier old-binary sample, with similar peak CLI RSS; no causal speed gain is claimed.

**IMPACT:** Claude's one read-only review is closed with both findings addressed. All 16 blast-radius integration tests, the formatter test, workspace clippy, formatting, and diff checks pass. The next step is a clean commit, then prepare v2.0.1 manifests, notes, full release gates, and Windows verification before publication.
