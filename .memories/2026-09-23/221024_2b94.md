---
id: checkpoint_2b944bce
timestamp: 2026-09-23T22:10:24.490Z
tags:
  - issue-3
  - project-root
  - 2.1.0
  - review-round-d
git:
  branch: feat/project-root
  commit: cf147ec
  worktree: /home/murphy/source/code-kb/.claude/worktrees/project-root
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Branch review round D (Task 1 worker): CLI refuses a home or filesystem root"
type: checkpoint
symbols:
  - main
---

## Branch review round D (Task 1 worker): CLI refuses a home or filesystem root

- **What:** `crates/code-kb-cli/src/main.rs` now calls `Workspace::from_project_root(<root>)` after the Scan branch and before `ensure_index_matches_extractor`. A `ProjectRootRefused` whose reason is not `NO_PROJECT_MARKER_REASON` (the home or a filesystem root) prints `Error: <text>. Run the command inside a project or pass --root <project path>.` and exits 1 without touching the index. Auto-create runs only when the same call accepts the root.
- **Why:** a leftover `~/.code-kb/artifact.db` under a dotfiles `~/.git` let CLI commands in `~/notes` rebuild, reconcile, and answer from the whole home, while the MCP tools refuse that root.
- **Kept:** a markerless root with `--db` or an existing index still works; `code-kb scan` is unchanged.
- **Tests:** new `test_cli_query_refuses_a_leftover_home_index_and_leaves_it_unchanged` (red first: the lookup answered from the home index). The round C test now expects the home refusal text in place of `Run code-kb scan first`.
- **Verification:** `cargo test -p code-kb-cli --test cli_test` 49 passed; clippy `-D warnings` clean; fmt clean.
