---
id: checkpoint_0ab69373
timestamp: 2026-09-23T14:37:43.247Z
tags:
  - release
  - v2.0.2
  - claude-review
  - qml
  - julie-3.5.0
git:
  branch: main
  commit: 0fb2625
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Claude reviewed the code-kb 2.0.2 candidate
type: decision
decision: Proceed with code-kb v2.0.2 release after a clean Claude review; defer
  the single low finding because its proposed pending-query change cannot affect
  the observed direct extractor relationship, which exists in both Julie 3.3.1
  and 3.5.0.
alternatives:
  - "Add recursive scope filtering to pending_target_predicate now: rejected
    because the reproduced nested QML call is a resolved extractor relationship,
    not a pending row, and the query change would not fix it."
  - "Block the release on the nested QML case: rejected because the same
    extractor row exists in the previously released 3.3.1 and the reviewer found
    no new medium/high defect."
evidence:
  - "Claude structured review: review_completed=true, one low finding, zero
    medium/high"
  - "Direct 3.3.1 and 3.5.0 extraction of nested QML: identical resolved inner
    -> helper row, no pending helper row"
next: Commit review report, fast-forward main after state reconciliation, push
  main, wait exact-commit CI, then tag and publish v2.0.2.
---

## Claude reviewed the code-kb 2.0.2 candidate

**WHAT:** One structured, read-only Claude CLI review covered the complete `main..abdfc83` diff. It completed with one low finding and no release blocker. The review result is recorded in `docs/reviews/2026-09-23-claude-v2.0.2.md` and the normalized JSON is in the perf evidence directory.

**WHY:** User explicitly requested a fresh Claude review before release.

**HOW:** Sent a redacted complete diff to an isolated review tree with read-only Claude tools. Verified its nested QML shadow concern against direct extractor artifacts: both 3.3.1 and 3.5.0 resolve `inner -> helper` directly with no pending row. The suggested pending SQL change would not affect it.

**IMPACT:** Review campaign closed clean at medium severity floor. The known nested-call limit is preexisting upstream behavior; continue release flow after committing the report and rechecking the source tree.
