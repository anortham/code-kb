---
id: checkpoint_af0688f6
timestamp: 2026-09-23T01:07:40.968Z
tags:
  - release
  - v2.0.1
  - candidate
  - verification
  - patch
git:
  branch: review/v2.0-live-dogfood
  commit: fd55468
  files:
    - .claude-plugin/marketplace.json
    - .claude-plugin/plugin.json
    - .codex-plugin/plugin.json
    - .github/workflows/release-binaries.yml
    - Cargo.lock
    - Cargo.toml
    - crates/code-kb-cli/Cargo.toml
    - docs/release-notes/v2.0.1.md
    - docs/site/index.html
    - plugin.json
actor:
  harness: codex-mcp-client
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: Prepared the local code-kb 2.0.1 release candidate
type: checkpoint
impact: Release candidate is locally verified except Windows on the final SHA
  and GitHub CI, which require a clean committed and pushed source.
evidence:
  - "cargo test --workspace --locked --no-fail-fast: 397 passed"
  - "node --test tests/plugin/*.test.cjs: 17 passed"
  - "cargo clippy --workspace --all-targets --locked -- -D warnings: exit 0"
  - "cargo package --workspace --no-verify --allow-dirty: core and CLI 2.0.1
    packaged"
  - "cargo audit --format json: 0 vulnerabilities, 0 warnings"
  - "gitleaks staged and ce801e2..HEAD scans: no leaks"
  - "target/release/code-kb --version: 2.0.1"
symbols:
  - McpServer::handle_call_tool_inner
  - compute_blast_radius_scoped_with_ids
next: Commit the staged candidate with this checkpoint; run Windows suite and
  preflight checks, then request only the approval needed for push and
  publication.
---

## Prepared the local code-kb 2.0.1 release candidate

**WHAT:** Bumped the workspace, CLI core dependency, four plugin/marketplace manifests, release workflow default, site badge, and Cargo.lock to 2.0.1. Drafted `docs/release-notes/v2.0.1.md` for the zero-limit, bounded test-prediction, wording, schema-description, and benchmark fixes. The reviewed code, including fixes for Claude's medium and low findings, is committed through `fd55468` and passed focused Windows verification.

**WHY:** The user requested a patch release after Claude CLI reviewed the v2.0 dogfooding changes. The v2.0.0 release is published; no v2.0.1 tag or crate exists.

**HOW:** Followed `docs/RELEASING.md` version anchors and ran the local release gates: 397 Rust tests, 17 plugin tests, workspace clippy and formatting, AGENTS/CLAUDE sync, both workspace crate package dry-runs, release build reporting `code-kb 2.0.1`, staged and branch-diff gitleaks scans with no findings, and cargo audit with zero vulnerabilities. A whole-directory secrets scan was stopped because the ignored `target/` directory is 65 GB; the targeted scans cover code intended for publication.

**IMPACT:** The candidate is staged locally on `review/v2.0-live-dogfood`. Commit it with this checkpoint, run full Windows verification on the clean SHA, then prepare the exact state and approval request for push, CI, tag, GitHub Release, and crates.io publication. Do not tag until CI passes on origin/main.
