---
id: checkpoint_6079e4d1
timestamp: 2026-09-19T00:07:22.568Z
tags:
  - find-references
  - identifiers
  - qualified-call-chain
  - task-7
  - fix-round-1
git:
  branch: main
  commit: eb6c9ba
  worktree: /home/murphy/source/code-kb
  files:
    - docs/plans/2026-09-18-qualified-call-chain-receivers.md
actor:
  harness: claude-code
  user: murphy
  git_user: Alan Northam
  git_email: anortham@gmail.com
summary: "Task 7 fix round 1: identifier receiver check compares the parent's name"
briefId: code-kb-lightweight-code-intelligence-engine-mcp-s
type: checkpoint
evidence:
  - "cargo test -p code-kb-core: 180 passed"
  - "references_test: 6 passed"
  - "repro refs: 3 calls rows, 0 member_access"
symbols:
  - find_references_internal
---

## Task 7 fix round 1: identifier receiver check compares the parent's name

**WHAT**
- Identifier fallback in `find_references_internal` (queries.rs): the exclusion now compares the target parent's *name* with the recorded receiver (`owner.name IS NOT (SELECT parent.name ...)`) instead of the owner's symbol id.
- New test `same_named_containers_with_same_named_members_keep_the_shared_caller` in `references_test.rs`: two C# `Settings` classes in namespaces `A` and `B`, each owning `Value`, and a reader of `Settings.Value`; callers of each `Value` (scoped by declaring file) include `Read`.

**WHY**
- External codex review found that the id comparison let two same-named containers with same-named members exclude each other's references (`A.Settings.Value` and `B.Settings.Value` both lost the caller).

**HOW**
- Red on 308015c (`src/A.cs: got []`), green after the one-line predicate change. `IS NOT` keeps the no-parent behaviour (a top-level target still drops rows whose receiver owns a same-named member).

**IMPACT**
- `cargo test -p code-kb-core`: 180 passed. Repro `refs AnnualAttestationNotification` still lists three `calls` rows and no `member_access` row.
