# Claude review of code-kb v2.0.2

The read-only Claude CLI review covered the complete `main..abdfc83` branch diff. It inspected the QML resolver, extractor pin, Go manifest test, build guard, plugin manifests, and release wiring. The structured review completed with one low-severity finding and no release blocker. No external-model policy block is declared in this repository; the outbound diff was redacted before dispatch.

## Finding and disposition

Claude noted that the new QML pending-call shadow guard checks variables and parameters owned directly by the caller. It suggested walking outer function scopes for a nested call. That is a possible gap in the predicate, but the proposed fix does not address the concrete QML case:

- A QML function `outer(root)` containing `inner() { root.helper() }` produces a direct `inner → helper` relationship, with no pending `helper` row.
- julie-extract 3.3.1 and 3.5.0 produce the same direct row for that source.
- `pending_target_predicate` runs only on pending rows, so changing it would not filter this preexisting extractor result.

The finding is deferred as an upstream extraction limit outside the 3.5.0 upgrade. Adding a recursive SQL scope walk to the pending-call hot path without a failing pending-row case would add query cost and leave the observed false caller in place.

## Review status

`external-reviewed`; one Claude invocation; lead confirmation complete; no open finding at or above medium severity. The local Linux, Windows, plugin, formatting, Clippy, and packaging gates passed before the review. The release still requires exact-commit GitHub CI after pushing `main`.

```text
REVIEW CAMPAIGN STATUS
state: clean
evidence: external-reviewed
round: 2/2
external_invocations: 1/1
open_critical_high: 0
open_medium_low: 1
open_above_floor: 0
campaign_closed: yes
```

## Issue #2 follow-up review

After commit `1fc0e7b`, a second read-only Claude CLI review covered the complete `main..HEAD` issue-fix diff. The redacted review completed with two low-severity observations and no correctness or security defect:

- The integration test checks that the namespace, class, constructor, and method appear, but does not snapshot every output line. This test failed before the formatter fix and passed afterward on Linux and Windows. Exact formatting is outside the reported failure, so no additional snapshot test is needed.
- The checkpoint names the Terraform repository and `OffboardingFormMapping`, both already present in the public issue. Its Azure DevOps mention identifies no organization or credential. The `next` field records the next step when the checkpoint was written, before the commit. No information needs removal.

Lead confirmation found no open finding at or above medium severity. The release remains paused at the user's request.

## Updated Terraform follow-up review

Claude reviewed commit `ca192db`, which fixed expression-bodied C# method skeletons, and found one medium and two low observations. Lead confirmation handled them as follows:

- The quote scanner could leave a signature open after a C# verbatim string or character literal and expose body text. Replaced it with a narrow method-declaration arrow check while keeping the existing brace rule. The regression fixture covers both literals.
- A C# field lambda was truncated by the unrestricted arrow check. Arrow trimming now applies only to C# method-like symbols, and the fixture checks that the field initializer remains intact.
- The suggested Dart body leak did not reproduce. A small indexed Dart file with an expression-bodied getter and method rendered both signatures without bodies.

The final formatter change passed the focused regression, all 400 Linux workspace tests, formatting, Clippy, and a release build. The release remains paused while newer issues are assessed.

## Final issue follow-up

Commit `1f3f87a` addresses the new issue #2 comment and issues #3 and #4. A final Claude CLI review of `e047986..1f3f87a` exited before producing a usable result; its review-tree and redaction preflight passed, but no finding or completion claim can be attributed to that call. A separate read-only reviewer found no concrete bug in the delta. The complete Linux and Windows suites passed 405 and 399 tests, respectively, and all 17 plugin tests passed. The final delta still needs a completed Claude review before release.
